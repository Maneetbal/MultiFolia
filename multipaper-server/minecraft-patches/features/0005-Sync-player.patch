From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Koyorice <87787929+Koyorice@users.noreply.github.com>
Date: Thu, 2 Oct 2025 18:30:52 +0800
Subject: [PATCH] Sync player


diff --git a/ca/spottedleaf/moonrise/patches/chunk_system/player/RegionizedPlayerChunkLoader.java b/ca/spottedleaf/moonrise/patches/chunk_system/player/RegionizedPlayerChunkLoader.java
index 45c927417d34ddd48c92116efd9b5777bfc52e35..3d49f6442aaefcff43acb1c93df79dd449b8f5b8 100644
--- a/ca/spottedleaf/moonrise/patches/chunk_system/player/RegionizedPlayerChunkLoader.java
+++ b/ca/spottedleaf/moonrise/patches/chunk_system/player/RegionizedPlayerChunkLoader.java
@@ -186,7 +186,7 @@ public final class RegionizedPlayerChunkLoader {
 
     public void addPlayer(final ServerPlayer player) {
         TickThread.ensureTickThread(player, "Cannot add player to player chunk loader async");
-        if (!((ChunkSystemServerPlayer)player).moonrise$isRealPlayer()) {
+        if (puregero.multipaper.MultiPaper.isExternalPlayer(player) || !((ChunkSystemServerPlayer)player).moonrise$isRealPlayer()) { // MultiPaper - Sync player
             return;
         }
 
@@ -211,7 +211,7 @@ public final class RegionizedPlayerChunkLoader {
 
     public void removePlayer(final ServerPlayer player) {
         TickThread.ensureTickThread(player, "Cannot remove player from player chunk loader async");
-        if (!((ChunkSystemServerPlayer)player).moonrise$isRealPlayer()) {
+        if (puregero.multipaper.MultiPaper.isExternalPlayer(player) || !((ChunkSystemServerPlayer)player).moonrise$isRealPlayer()) { // MultiPaper - Sync player
             return;
         }
 
diff --git a/net/minecraft/network/Connection.java b/net/minecraft/network/Connection.java
index 96900bee726db8a0b7d3072076e7b07f4a3492f4..4bf01d76786dd17fa8745400cb6131bdd309e0bf 100644
--- a/net/minecraft/network/Connection.java
+++ b/net/minecraft/network/Connection.java
@@ -652,6 +652,12 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
     public void disconnect(final DisconnectionDetails details) {
         this.preparing = false; // Spigot
         this.clearPacketQueue(); // Paper - Optimize network
+        // MultiPaper start - Sync player
+        net.minecraft.server.level.ServerPlayer player = this.getPlayer();
+        if (player != null && player.hasRegisteredOnMaster) {
+            puregero.multipaper.MultiPaper.sendPlayerDisconnect(player);
+        }
+        // MultiPaper end - Sync player
         if (this.channel == null) {
             this.delayedDisconnect = details;
         }
diff --git a/net/minecraft/network/FriendlyByteBuf.java b/net/minecraft/network/FriendlyByteBuf.java
index 12cd230ebe97dab655f8546948f06b93f0babf1f..cfeab241e6192227811adabba53e1d48d92efeda 100644
--- a/net/minecraft/network/FriendlyByteBuf.java
+++ b/net/minecraft/network/FriendlyByteBuf.java
@@ -77,6 +77,7 @@ public class FriendlyByteBuf extends ByteBuf {
     private static final int MAX_PUBLIC_KEY_HEADER_SIZE = 256;
     private static final int MAX_PUBLIC_KEY_LENGTH = 512;
     private static final Gson GSON = new Gson();
+    public long maxNbtSize = 2097152L; // MultiPaper - Sync player
     // Paper start - Track codec depth
     public boolean trackCodecDepth;
     public byte codecDepth;
@@ -547,7 +548,7 @@ public class FriendlyByteBuf extends ByteBuf {
     }
 
     public @Nullable CompoundTag readNbt() {
-        return readNbt(this);
+        return (CompoundTag) readNbt(this, NbtAccounter.create(this.maxNbtSize)); // MultiPaper - Sync player
     }
 
     public static @Nullable CompoundTag readNbt(final ByteBuf input) {
diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
index 5e3094f9521bc9fba41bc8a512ce0412462375d3..b4dad477fd562b26db7fd7a1764f54491bee32b8 100644
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1713,6 +1713,8 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
             this.status = this.buildServerStatus();
         }
 
+        puregero.multipaper.MultiPaper.tick(); // MultiPaper - Sync player
+
         this.ticksUntilAutosave--;
         // Paper start - Incremental chunk and player saving
         int playerSaveInterval = io.papermc.paper.configuration.GlobalConfiguration.get().playerAutoSave.rate;
diff --git a/net/minecraft/server/PlayerAdvancements.java b/net/minecraft/server/PlayerAdvancements.java
index 15898799e68e9e5f5c182be97f4112ede132715f..abe9d83ec84d9f84ab754f6da37b4dac6a0d3f3c 100644
--- a/net/minecraft/server/PlayerAdvancements.java
+++ b/net/minecraft/server/PlayerAdvancements.java
@@ -110,8 +110,12 @@ public class PlayerAdvancements {
     }
 
     private void load(final ServerAdvancementManager manager) {
-        if (Files.isRegularFile(this.playerSavePath)) {
-            try (Reader reader = Files.newBufferedReader(this.playerSavePath, StandardCharsets.UTF_8)) {
+        // MultiPaper start - Sync player
+        byte[] bytes = puregero.multipaper.ExternalPlayer.loadedAdvancements.remove(this.player.getUUID());
+        if (bytes == null) bytes = puregero.multipaper.MultiPaper.readFile(this.playerSavePath.toString());
+        if (bytes != null && bytes.length != 0) {
+            try (Reader reader = new java.io.InputStreamReader(new java.io.ByteArrayInputStream(bytes), StandardCharsets.UTF_8)) {
+        // MultiPaper end - Sync player
                 JsonElement json = StrictJsonParser.parse(reader);
                 PlayerAdvancements.Data data = this.codec.parse(JsonOps.INSTANCE, json).getOrThrow(JsonParseException::new);
                 this.applyFrom(manager, data);
@@ -126,11 +130,24 @@ public class PlayerAdvancements {
         this.registerListeners(manager);
     }
 
+    // MultiPaper start - Sync player
+    public JsonElement toJson() {
+        return this.codec.encodeStart(JsonOps.INSTANCE, this.asData()).getOrThrow();
+    }
+    // MultiPaper end - Sync player
+
     public void save() {
         if (org.spigotmc.SpigotConfig.disableAdvancementSaving) return; // Spigot
         JsonElement json = this.codec.encodeStart(JsonOps.INSTANCE, this.asData()).getOrThrow();
 
         try {
+            // MultiPaper start - Sync player
+            if (true) {
+                puregero.multipaper.MultiPaper.writeFile(this.playerSavePath.toString(), GSON.toJson(json).getBytes(StandardCharsets.UTF_8));
+                return;
+            }
+            // MultiPaper end - Sync player
+
             FileUtil.createDirectoriesSafe(this.playerSavePath.getParent());
 
             try (Writer outputWriter = Files.newBufferedWriter(this.playerSavePath, StandardCharsets.UTF_8)) {
@@ -177,10 +194,11 @@ public class PlayerAdvancements {
                 return false;
             }
             // Paper end - Add PlayerAdvancementCriterionGrantEvent
+            puregero.multipaper.externalserverprotocol.AdvancementGrantProgressPacket.onAdvancementGrantProgress(this.player.getUUID(), criterion, holder.id()); // MultiPaper - Sync player
             this.unregisterListeners(holder);
             this.progressChanged.add(holder);
             result = true;
-            if (!wasDone && progress.isDone()) {
+            if (!wasDone && progress.isDone() && puregero.multipaper.MultiPaper.isRealPlayer(this.player)) { // MultiPaper - Sync player - only broadcast advancement message for our players
                 // Paper start - Add Adventure message to PlayerAdvancementDoneEvent
                 final net.kyori.adventure.text.Component message = holder.value().display().flatMap(info -> {
                     return java.util.Optional.ofNullable(
diff --git a/net/minecraft/server/ServerScoreboard.java b/net/minecraft/server/ServerScoreboard.java
index 7ffd683c42557052d7bfa963d87e68f4e8f62d6c..103803e8de2d9c04630731042e2c015cd1b838f6 100644
--- a/net/minecraft/server/ServerScoreboard.java
+++ b/net/minecraft/server/ServerScoreboard.java
@@ -221,8 +221,8 @@ public class ServerScoreboard extends Scoreboard {
         }
     }
 
-    public List<Packet<?>> getStartTrackingPackets(final Objective objective) {
-        List<Packet<?>> packets = Lists.newArrayList();
+    public List<Packet<? super net.minecraft.network.protocol.game.ClientGamePacketListener>> getStartTrackingPackets(final Objective objective) { // MultiPaper - Sync player
+        List<Packet<? super net.minecraft.network.protocol.game.ClientGamePacketListener>> packets = Lists.newArrayList(); // MultiPaper - Sync player
         packets.add(new ClientboundSetObjectivePacket(objective, ClientboundSetObjectivePacket.METHOD_ADD));
 
         for (DisplaySlot slot : DisplaySlot.values()) {
@@ -243,7 +243,7 @@ public class ServerScoreboard extends Scoreboard {
     }
 
     public void startTrackingObjective(final Objective objective) {
-        List<Packet<?>> packets = this.getStartTrackingPackets(objective);
+        List<Packet<? super net.minecraft.network.protocol.game.ClientGamePacketListener>> packets = this.getStartTrackingPackets(objective); // MultiPaper - Sync player
 
         for (ServerPlayer player : this.server.getPlayerList().getPlayers()) {
             if (player.getBukkitEntity().getScoreboard().getHandle() != this) continue; // CraftBukkit - Only players on this board
@@ -310,12 +310,29 @@ public class ServerScoreboard extends Scoreboard {
         }
     }
     // CraftBukkit start - Send to players
-    private void broadcastAll(Packet<?> packet) {
+    private void broadcastAll(Packet<? super net.minecraft.network.protocol.game.ClientGamePacketListener> packet) { // MultiPaper - Sync player
+        // MultiPaper start - Sync player
+        // server hasn't started yet
+        if (org.bukkit.Bukkit.getScoreboardManager() == null) return;
+        boolean mainScoreboard = this == ((org.bukkit.craftbukkit.scoreboard.CraftScoreboard) org.bukkit.Bukkit.getScoreboardManager().getMainScoreboard()).getHandle();
+        // MultiPaper end - Sync player
         for (ServerPlayer serverPlayer : this.server.getPlayerList().getPlayers()) {
-            if (serverPlayer.getBukkitEntity().getScoreboard().getHandle() == this) {
+            if (serverPlayer.getBukkitEntity().getScoreboard().getHandle() == this && !(mainScoreboard && puregero.multipaper.MultiPaper.isExternalPlayer(serverPlayer))) { // MultiPaper - Sync player - don't external scoreboards that we sync to other players
                 serverPlayer.connection.send(packet);
             }
         }
+
+        // MultiPaper start - Sync player
+        // only sync the main scoreboard for now
+        if (mainScoreboard && puregero.multipaper.config.MultiPaperConfiguration.get().syncSettings.syncScoreboards && !puregero.multipaper.externalserverprotocol.ScoreboardUpdatePacket.updating) {
+            if (packet instanceof ClientboundSetObjectivePacket setObjectivePacket) {
+                Objective objective = this.getObjective(setObjectivePacket.getObjectiveName());
+                puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(new puregero.multipaper.externalserverprotocol.ScoreboardUpdatePacket(null, objective != null ? objective.getCriteria() : null, packet));
+            } else {
+                puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(new puregero.multipaper.externalserverprotocol.ScoreboardUpdatePacket(null, packet));
+            }
+        }
+        // MultiPaper end - Sync player
     }
     // CraftBukkit end
 }
diff --git a/net/minecraft/server/dedicated/DedicatedPlayerList.java b/net/minecraft/server/dedicated/DedicatedPlayerList.java
index 8cc4fe9feb57902a4b9a6a26e6175ac00a2cef65..905c3b620292ee91b938fd35fcfc8f365be9587a 100644
--- a/net/minecraft/server/dedicated/DedicatedPlayerList.java
+++ b/net/minecraft/server/dedicated/DedicatedPlayerList.java
@@ -22,15 +22,19 @@ public class DedicatedPlayerList extends PlayerList {
     public void loadAndSaveFiles() {
         // Paper end - fix converting txt to json file
         this.loadUserBanList();
-        this.saveUserBanList();
+        // this.saveUserBanList(); // MultiPaper - Sync player
         this.loadIpBanList();
-        this.saveIpBanList();
+        // this.saveIpBanList(); // MultiPaper - Sync player
         this.loadOps();
         this.loadWhiteList();
+        // MultiPaper start - Sync player
+        /*
         this.saveOps();
         if (!this.getWhiteList().getFile().exists()) {
             this.saveWhiteList();
         }
+        */
+        // MultiPaper end - Sync player
     }
 
     @Override
diff --git a/net/minecraft/server/level/ChunkMap.java b/net/minecraft/server/level/ChunkMap.java
index 7e9361407042ddaabe3f66ee8957ae84b6ef2317..9a26b6ecf21f53c7e00fc8b1b1d907efd51cd6c2 100644
--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -892,7 +892,7 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
     }
 
     private boolean skipPlayer(final ServerPlayer player) {
-        return player.isSpectator() && !this.level.getGameRules().get(GameRules.SPECTATORS_GENERATE_CHUNKS);
+        return puregero.multipaper.MultiPaper.isExternalPlayer(player) || (player.isSpectator() && !this.level.getGameRules().get(GameRules.SPECTATORS_GENERATE_CHUNKS)); // MultiPaper - Sync player - skip external players
     }
 
     private void updatePlayerStatus(final ServerPlayer player, final boolean added) {
@@ -1071,6 +1071,13 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
                 if (tracker == null) {
                     continue;
                 }
+            // MultiPaper start - Sync player - don't track for external players
+            if (entity instanceof puregero.multipaper.ExternalPlayer) {
+                tracker.updatePlayers(this.level.players());
+                tracker.serverEntity.sendChanges();
+                continue;
+            }
+            // MultiPaper end - Sync player - don't track for external players
                 ((ca.spottedleaf.moonrise.patches.entity_tracker.EntityTrackerTrackedEntity)tracker).moonrise$tick(((ca.spottedleaf.moonrise.patches.chunk_system.entity.ChunkSystemEntity)entity).moonrise$getChunkData().nearbyPlayers);
                 if (((ca.spottedleaf.moonrise.patches.entity_tracker.EntityTrackerTrackedEntity)tracker).moonrise$hasPlayers()
                     || ((ca.spottedleaf.moonrise.patches.chunk_system.entity.ChunkSystemEntity)entity).moonrise$getChunkStatus().isOrAfter(FullChunkStatus.ENTITY_TICKING)) {
@@ -1378,7 +1385,7 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
 
         public void updatePlayer(final ServerPlayer player) {
             org.spigotmc.AsyncCatcher.catchOp("player tracker update"); // Spigot
-            if (player != this.entity) {
+            if (player != this.entity && puregero.multipaper.MultiPaper.isRealPlayer(player)) { // MultiPaper - Sync player - don't track for external players
                 // Paper start - remove allocation of Vec3D here
                 // Vec3 deltaToPlayer = player.position().subtract(this.entity.position());
                 double deltaToPlayerX = player.getX() - this.entity.getX();
diff --git a/net/minecraft/server/level/ServerEntity.java b/net/minecraft/server/level/ServerEntity.java
index 60af82925d2f7e605bf7340cf01f1a5668f4d725..81ed0bc7b685f34101085b986ec391ba70ab29af 100644
--- a/net/minecraft/server/level/ServerEntity.java
+++ b/net/minecraft/server/level/ServerEntity.java
@@ -253,7 +253,7 @@ public class ServerEntity {
             // CraftBukkit start - Create PlayerVelocity event
             boolean cancelled = false;
 
-            if (this.entity instanceof ServerPlayer) {
+            if (this.entity instanceof ServerPlayer && puregero.multipaper.MultiPaper.isRealPlayer(this.entity)) { // MultiPaper - Sync player
                 org.bukkit.entity.Player player = (org.bukkit.entity.Player) this.entity.getBukkitEntity();
                 org.bukkit.util.Vector velocity = player.getVelocity();
 
diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index dbc883141d7a6b653ef05746b5b458db2d3856dd..ba41bdd1d63b9f677c471f4ec5ba9ee3e68672ea 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -977,7 +977,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
 
     private void wakeUpAllPlayers() {
         this.sleepStatus.removeAllSleepers();
-        this.players.stream().filter(LivingEntity::isSleeping).collect(Collectors.toList()).forEach(player -> player.stopSleepInBed(false, false));
+        this.players.stream().filter(LivingEntity::isSleeping).filter(puregero.multipaper.MultiPaper::isRealPlayer).collect(Collectors.toList()).forEach(player -> player.stopSleepInBed(false, false)); // MultiPaper - Sync player - only wake up local players
     }
 
     // Paper start - optimise random ticking
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index d1dce39cf916aee52858b5769f340c1a9237e7ab..46cc56bb08dd709af5bf521d1d3b822b9f83d882 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -231,7 +231,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     private static final boolean DEFAULT_SEEN_CREDITS = false;
     private static final boolean DEFAULT_SPAWN_EXTRA_PARTICLES_ON_FALL = false;
     public ServerGamePacketListenerImpl connection;
-    private final MinecraftServer server;
+    public final MinecraftServer server; // MultiPaper - Sync player - make public
     public final ServerPlayerGameMode gameMode;
     private final PlayerAdvancements advancements;
     private final ServerStatsCounter stats;
@@ -272,6 +272,10 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     private boolean allowsListing;
     private boolean spawnExtraParticlesOnFall = false;
     public WardenSpawnTracker wardenSpawnTracker = new WardenSpawnTracker();
+    // MultiPaper start - Sync player
+    public boolean hasRegisteredOnMaster = false;
+    public boolean didMultiPaperJoin = false;
+    // MultiPaper end - Sync player
     private @Nullable BlockPos raidOmenPosition;
     private Vec3 lastKnownClientMovement = Vec3.ZERO;
     private Input lastClientInput = Input.EMPTY;
@@ -378,6 +382,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         }
     };
     private @Nullable RemoteChatSession chatSession;
+    public puregero.multipaper.@Nullable ExternalServer openContainer; // MultiPaper - Sync player
     public final @Nullable Object object;
     private final CommandSource commandSource = new CommandSource() {
         @Override
@@ -410,13 +415,17 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     private Set<DebugSubscription<?>> requestedDebugSubscriptions = Set.of();
     private int containerCounter;
     public boolean wonGame;
-    private int containerUpdateDelay; // Paper - Configurable container update tick rate
+    // MultiPaper start - Sync player
+    public boolean destroyAndAckHandledByExternalServer = false;
+    public int containerUpdateDelay; // Paper - Configurable container update tick rate // make public
+    // MultiPaper end - Sync player
     public long loginTime; // Paper - Replace OfflinePlayer#getLastPlayed
     public int patrolSpawnDelay; // Paper - Pillager patrol spawn settings and per player options
     // Paper start - cancellable death event
     public boolean queueHealthUpdatePacket;
     public net.minecraft.network.protocol.game.@Nullable ClientboundSetHealthPacket queuedHealthUpdatePacket;
     // Paper end - cancellable death event
+    public static final java.util.Map<UUID, Integer> persistentEntityIds = new java.util.concurrent.ConcurrentHashMap<>(); // MultiPaper - Sync player - persistent entity ids for players across servers
     // Paper start - Optional per player mob spawns
     public static final int MOBCATEGORY_TOTAL_ENUMS = net.minecraft.world.entity.MobCategory.values().length;
     public final int[] mobCounts = new int[MOBCATEGORY_TOTAL_ENUMS];
@@ -477,6 +486,18 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
     public ServerPlayer(final MinecraftServer server, final ServerLevel level, final GameProfile gameProfile, final ClientInformation clientInformation) {
         super(level, gameProfile);
+        // MultiPaper start - Sync player
+        // persistent entity ids for players across servers
+        if (puregero.multipaper.config.MultiPaperConfiguration.get().syncSettings.persistentPlayerEntityIds) {
+            if (!(this instanceof puregero.multipaper.ExternalPlayer)) {
+                Integer value = persistentEntityIds.get(gameProfile.id());
+                if (value != null) {
+                    setId(value);
+                }
+            }
+            persistentEntityIds.put(gameProfile.id(), getId());
+        }
+        // MultiPaper end - Sync player
         this.server = server;
         this.textFilter = server.createTextFilterForPlayer(this);
         this.gameMode = server.createGameModeForPlayer(this);
@@ -492,6 +513,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         this.bukkitPickUpLoot = true;
         this.maxHealthCache = this.getMaxHealth();
         // CraftBukkit end
+        this.enderChestInventory.listener = new puregero.multipaper.MultiPaperEnderChestHandler(this); // MultiPaper - Sync player
     }
 
     @Override
@@ -693,6 +715,21 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         this.lastSentExp = -1;
     }
 
+    // MultiPaper start - Sync player
+    public float lastExperienceProgress = 0;
+    public int lastTotalExperience = 0;
+    public int lastExperienceLevel = 0;
+
+    public void syncExperience() {
+        if (experienceProgress != lastExperienceProgress || totalExperience != lastTotalExperience || experienceLevel != lastExperienceLevel) {
+            lastExperienceProgress = experienceProgress;
+            lastTotalExperience = totalExperience;
+            lastExperienceLevel = experienceLevel;
+            puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(this, new puregero.multipaper.externalserverprotocol.PlayerExperienceUpdatePacket(this.uuid, experienceProgress, totalExperience, experienceLevel));
+        }
+    }
+    // MultiPaper end - Sync player
+
     public void initMenu(final AbstractContainerMenu container) {
         container.addSlotListener(this.containerListener);
         container.setSynchronizer(this.containerSynchronizer);
@@ -943,12 +980,12 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
                 float saturation = this.foodData.getSaturationLevel();
                 if (saturation < 20.0F) {
-                    this.foodData.setSaturation(saturation + 1.0F);
+                    this.foodData.setSaturation(this, saturation + 1.0F, true); // MultiPaper - Sync player
                 }
             }
 
             if (this.tickCount % 10 == 0 && this.foodData.needsFood()) {
-                this.foodData.setFoodLevel(this.foodData.getFoodLevel() + 1);
+                this.foodData.setFoodLevel(this, this.foodData.getFoodLevel() + 1, true); // MultiPaper - Sync player
             }
         }
     }
@@ -2397,6 +2434,8 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
             }
         }
 
+        puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(this, new puregero.multipaper.externalserverprotocol.PlayerChangeGamemodePacket(this)); // MultiPaper - Sync player
+
         this.onUpdateAbilities();
         this.updateEffectVisibility();
         return event; // Paper - Expand PlayerGameModeChangeEvent
@@ -2703,6 +2742,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
             this.connection.send(new ClientboundSetCameraPacket(this.camera));
             this.connection.resetPosition();
+            if (!puregero.multipaper.externalserverprotocol.PlayerSetCameraPacket.handlingSetCamera) puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(this, new puregero.multipaper.externalserverprotocol.PlayerSetCameraPacket(this, newCamera)); // MultiPaper - Sync player
         }
     }
 
@@ -2804,6 +2844,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         }
 
         this.respawnConfig = respawnConfig;
+        puregero.multipaper.externalserverprotocol.PlayerSetRespawnPosition.broadcastRespawnPosition(this); // MultiPaper - Sync player
         return true;
         // Paper end - Add PlayerSetSpawnEvent
     }
@@ -3266,6 +3307,20 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
             this.giveExperiencePoints(this.newExp);
         }
         this.keepLevel = false;
+        // MultiPaper start - Sync player
+        // syncing experience after reset
+        if (puregero.multipaper.MultiPaper.isRealPlayer(this)) {
+            // force resync of local player experience onto other servers if it's non-zero
+            this.lastExperienceProgress = 0;
+            this.lastExperienceLevel = 0;
+            this.lastTotalExperience = 0;
+        } else {
+            // don't sync experience for external players
+            this.lastExperienceProgress = this.experienceProgress;
+            this.lastExperienceLevel = this.experienceLevel;
+            this.lastTotalExperience = this.totalExperience;
+        }
+        // MultiPaper end - Sync player
         this.setDeltaMovement(0, 0, 0); // CraftBukkit - SPIGOT-6948: Reset velocity on death
         this.skipDropExperience = false; // CraftBukkit - SPIGOT-7462: Reset experience drop skip, so that further deaths drop xp
     }
diff --git a/net/minecraft/server/level/ServerPlayerGameMode.java b/net/minecraft/server/level/ServerPlayerGameMode.java
index 185a14c96741d9c394c07b1c7747e908282e9452..879d992a7846044b26fb0a47c03c47211aaeebe3 100644
--- a/net/minecraft/server/level/ServerPlayerGameMode.java
+++ b/net/minecraft/server/level/ServerPlayerGameMode.java
@@ -324,6 +324,14 @@ public class ServerPlayerGameMode {
     }
 
     public void destroyAndAck(final BlockPos pos, final int sequence, final String exitId) {
+        // MultiPaper start - Sync player
+        ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder newChunkHolder = puregero.multipaper.MultiPaper.getChunkHolder(this.level, pos);
+        if (puregero.multipaper.MultiPaper.isChunkExternal(newChunkHolder)) {
+            newChunkHolder.externalOwner.getConnection().send(new puregero.multipaper.externalserverprotocol.DestroyAndAckPacket(this.player.getUUID(), pos, sequence, exitId));
+            this.player.destroyAndAckHandledByExternalServer = true;
+            return;
+        }
+        // MultiPaper end - Sync player
         if (this.destroyBlock(pos)) {
             this.debugLogging(pos, true, sequence, exitId);
         } else {
@@ -333,6 +341,13 @@ public class ServerPlayerGameMode {
     }
 
     public boolean destroyBlock(final BlockPos pos) {
+        // MultiPaper start - Sync player
+        ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder newChunkHolder = puregero.multipaper.MultiPaper.getChunkHolder(this.level, pos);
+        if (puregero.multipaper.MultiPaper.isChunkExternal(newChunkHolder)) {
+            newChunkHolder.externalOwner.getConnection().send(new puregero.multipaper.externalserverprotocol.DestroyBlockPacket(this.player.getUUID(), pos));
+            return false;
+        }
+        // MultiPaper end - Sync player
         BlockState state = this.level.getBlockState(pos);
         // CraftBukkit start - fire BlockBreakEvent
         org.bukkit.block.Block bblock = org.bukkit.craftbukkit.block.CraftBlock.at(this.level, pos);
diff --git a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
index 048bbab91c4614f8935797b389594c1bdd4542b0..69c13b8ba563d7ebc3584632f6791cf6c18489e8 100644
--- a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
@@ -407,6 +407,7 @@ public abstract class ServerCommonPacketListenerImpl implements ServerCommonPack
 
         Component reason;
         Component leaveMessage;
+        boolean external = this.connection instanceof puregero.multipaper.ExternalPlayer.ExternalPlayerConnection; // MultiPaper - Sync player
         if (this instanceof ServerGamePacketListenerImpl serverGamePacketListener) {
             net.kyori.adventure.text.Component rawLeaveMessage = net.kyori.adventure.text.Component.translatable("multiplayer.player.left", net.kyori.adventure.text.format.NamedTextColor.YELLOW, io.papermc.paper.configuration.GlobalConfiguration.get().messages.useDisplayNameInQuitMessage ? serverGamePacketListener.player.getBukkitEntity().displayName() : net.kyori.adventure.text.Component.text(serverGamePacketListener.player.getScoreboardName())); // Paper - Adventure
 
@@ -419,7 +420,7 @@ public abstract class ServerCommonPacketListenerImpl implements ServerCommonPack
 
             );
 
-            if (this.cserver.getServer().isRunning()) {
+            if (external && this.cserver.getServer().isRunning()) { // MultiPaper - Sync player - don't run kick event for external disconnects
                 this.cserver.getPluginManager().callEvent(event);
             }
 
@@ -443,10 +444,16 @@ public abstract class ServerCommonPacketListenerImpl implements ServerCommonPack
         }
 
         // Send the possibly modified leave message
-        this.disconnect0(new DisconnectionDetails(reason, details.report(), details.bugReportLink(), java.util.Optional.ofNullable(leaveMessage), details.disconnectionReason()));
+        this.disconnect0(new DisconnectionDetails(reason, details.report(), details.bugReportLink(), java.util.Optional.ofNullable(leaveMessage), details.disconnectionReason()), external); // MultiPaper - Sync player
     }
 
-    private void disconnect0(final DisconnectionDetails details) {
+    // MultiPaper start - Sync player
+    private void disconnect0(final DisconnectionDetails details, final boolean external) {
+        if (external) {
+            this.onDisconnect(details);
+            return;
+        }
+    // MultiPaper end - Sync player
         this.connection.send(new ClientboundDisconnectPacket(details.reason()), PacketSendListener.thenRun(() -> this.connection.disconnect(details)));
         this.connection.setReadOnly();
         this.connection.handleConnectionDisconnectOnNextTick = true; // Paper - Force kill connection ticking. Let this close the connection
diff --git a/net/minecraft/server/network/ServerGamePacketListenerImpl.java b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
index 817ec3f235dc820bd9ff9b4ad34b90681a0efb89..95fded8b2e64cb5d6c91e3f05a896b6c6aa2f92d 100644
--- a/net/minecraft/server/network/ServerGamePacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
@@ -271,7 +271,7 @@ public class ServerGamePacketListenerImpl
     public ServerPlayer player;
     public final PlayerChunkSender chunkSender;
     private int tickCount;
-    private int ackBlockChangesUpTo = -1;
+    public int ackBlockChangesUpTo = -1; // MultiPaper - Sync player - make public
     private final TickThrottler dropSpamThrottler = new TickThrottler(20, 1480);
     private final TickThrottler chatSpamThrottler;
     private final TickThrottler commandSpamThrottler;
@@ -379,12 +379,8 @@ public class ServerGamePacketListenerImpl
 
         if (this.server.isPaused() || !this.tickPlayer()) {
             this.keepConnectionAlive();
-            this.chatSpamThrottler.tick();
-            this.commandSpamThrottler.tick();
-            this.dropSpamThrottler.tick();
-            this.tabSpamThrottler.tick(); // Paper - configurable tab spam limits
-            this.recipeSpamPackets.tick(); // Paper - auto recipe limit
-            if (this.player.getLastActionTime() > 0L
+            this.reduceSpamCounters(); // MultiPaper - Sync player - move to reduceSpamCounters()
+            if (puregero.multipaper.MultiPaper.isRealPlayer(this.player) && this.player.getLastActionTime() > 0L // MultiPaper - Sync player - only kick local afk players
                 && this.server.playerIdleTimeout() > 0
                 && Util.getMillis() - this.player.getLastActionTime() > TimeUnit.MINUTES.toMillis(this.server.playerIdleTimeout())
                 && !this.player.wonGame) {
@@ -399,6 +395,16 @@ public class ServerGamePacketListenerImpl
         }
     }
 
+    // MultiPaper start - Sync player
+    public void reduceSpamCounters() {
+        this.chatSpamThrottler.tick();
+        this.commandSpamThrottler.tick();
+        this.dropSpamThrottler.tick();
+        this.tabSpamThrottler.tick(); // Paper - configurable tab spam limits
+        this.recipeSpamPackets.tick(); // Paper - auto recipe limit
+    }
+    // MultiPaper end - Sync player
+
     private boolean tickPlayer() {
         this.resetPosition();
         this.player.xo = this.player.getX();
@@ -821,6 +827,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleAcceptPlayerLoad(final ServerboundPlayerLoadedPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        puregero.multipaper.MultiPaperPlayerHandler.handleAcceptPlayerLoad(this.player, packet); // MultiPaper - Sync player
         this.markClientLoaded(false); // Paper - Add PlayerLoadedWorldEvent
     }
 
@@ -842,6 +849,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleRecipeBookChangeSettingsPacket(final ServerboundRecipeBookChangeSettingsPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet); // MultiPaper - Sync player
         CraftEventFactory.callRecipeBookSettingsEvent(this.player, packet.getBookType(), packet.isOpen(), packet.isFiltering()); // CraftBukkit
         this.player.getRecipeBook().setBookSetting(packet.getBookType(), packet.isOpen(), packet.isFiltering());
     }
@@ -1136,6 +1144,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleRenameItem(final ServerboundRenameItemPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet)) return; // MultiPaper - Sync player
         if (this.player.containerMenu instanceof AnvilMenu menu) {
             if (!menu.stillValid(this.player)) {
                 LOGGER.debug("Player {} interacted with invalid menu {}", this.player, menu);
@@ -1149,6 +1158,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleSetBeaconPacket(final ServerboundSetBeaconPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet)) return; // MultiPaper - Sync player
         if (this.player.containerMenu instanceof BeaconMenu menu) {
             if (!this.player.containerMenu.stillValid(this.player)) {
                 LOGGER.debug("Player {} interacted with invalid menu {}", this.player, this.player.containerMenu);
@@ -2112,7 +2122,10 @@ public class ServerGamePacketListenerImpl
                     this.player.gameMode.captureSentBlockEntities = true;
                     // Paper end - Send block entities after destroy prediction
                     this.player.gameMode.handleBlockBreakAction(pos, action, packet.getDirection(), this.player.level().getMaxY(), packet.getSequence());
-                    this.ackBlockChangesUpTo(packet.getSequence());
+                    // MultiPaper start - Sync player
+                    if (this.player.destroyAndAckHandledByExternalServer) this.player.destroyAndAckHandledByExternalServer = false;
+                    else this.ackBlockChangesUpTo(packet.getSequence());
+                    // MultiPaper end - Sync player
                     // Paper start - Send block entities after destroy prediction
                     this.player.gameMode.captureSentBlockEntities = false;
                     // If a block entity was modified speedup the block change ack to avoid the block entity
@@ -2175,6 +2188,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleUseItemOn(final ServerboundUseItemOnPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleInteractEvent(this.player, packet)) return; // MultiPaper - Sync player
         if (this.player.isImmobile()) return; // CraftBukkit
         if (!this.checkLimit(packet.timestamp)) return; // Spigot - check limit
         if (this.hasClientLoaded()) {
@@ -2251,6 +2265,11 @@ public class ServerGamePacketListenerImpl
 
                             this.send(new ClientboundBlockUpdatePacket(level, pos));
                             this.send(new ClientboundBlockUpdatePacket(level, pos.relative(direction)));
+                            // MultiPaper start - Sync player
+                            // broadcast block change to other servers as they may be out of sync
+                            level.getChunkSource().blockChanged(pos);
+                            level.getChunkSource().blockChanged(pos.relative(direction));
+                            // MultiPaper end - Sync player
                         }
                         if (io.papermc.paper.configuration.GlobalConfiguration.get().unsupportedSettings.updateEquipmentOnPlayerActions) this.player.detectEquipmentUpdates(); // Paper - Force update attributes.
                         // Paper - Remove unused warning
@@ -2263,6 +2282,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleUseItem(final ServerboundUseItemPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleUseItemEvent(this.player, packet)) return; // MultiPaper - Sync player
         if (this.player.isImmobile()) return; // CraftBukkit
         if (!this.checkLimit(packet.timestamp)) return; // Spigot - check limit
         if (this.hasClientLoaded()) {
@@ -2375,7 +2395,7 @@ public class ServerGamePacketListenerImpl
         this.player.disconnect();
         // Paper start - Adventure
         quitMessage = quitMessage == null ? this.server.getPlayerList().remove(this.player) : this.server.getPlayerList().remove(this.player, quitMessage); // Paper - pass in quitMessage to fix kick message not being used
-        if ((quitMessage != null) && !quitMessage.equals(net.kyori.adventure.text.Component.empty())) {
+        if ((quitMessage != null) && !quitMessage.equals(net.kyori.adventure.text.Component.empty()) && puregero.multipaper.MultiPaper.isRealPlayer(this.player)) { // MultiPaper - Sync player - Only run for real players
             this.server.getPlayerList().broadcastSystemMessage(PaperAdventure.asVanilla(quitMessage), false);
             // Paper end - Adventure
         }
@@ -2390,6 +2410,7 @@ public class ServerGamePacketListenerImpl
         }
 
         this.ackBlockChangesUpTo = Math.max(packetSequenceNr, this.ackBlockChangesUpTo);
+        puregero.multipaper.MultiPaperAckBlockChangesHandler.onAckNeeded(this.player); // MultiPaper - Sync player
     }
 
     @Override
@@ -2397,9 +2418,10 @@ public class ServerGamePacketListenerImpl
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
         if (this.player.isImmobile()) return; // CraftBukkit
         if (packet.getSlot() >= 0 && packet.getSlot() < Inventory.getSelectionSize()) {
+            puregero.multipaper.MultiPaperPlayerHandler.handleSetCarriedItem(this.player, packet); // MultiPaper - Sync player
             if (packet.getSlot() == this.player.getInventory().getSelectedSlot()) { return; } // Paper - don't fire itemheldevent when there wasn't a slot change
             PlayerItemHeldEvent event = new PlayerItemHeldEvent(this.getCraftPlayer(), this.player.getInventory().getSelectedSlot(), packet.getSlot());
-            this.cserver.getPluginManager().callEvent(event);
+            if (puregero.multipaper.MultiPaper.isRealPlayer(this.player)) this.cserver.getPluginManager().callEvent(event); // MultiPaper - Sync player - only fire PlayerItemHeldEvent for local players
             if (event.isCancelled()) {
                 this.send(new ClientboundSetHeldSlotPacket(this.player.getInventory().getSelectedSlot()));
                 this.player.resetLastActionTime();
@@ -2412,7 +2434,15 @@ public class ServerGamePacketListenerImpl
 
             this.player.getInventory().setSelectedSlot(packet.getSlot());
             this.player.resetLastActionTime();
-            if (io.papermc.paper.configuration.GlobalConfiguration.get().unsupportedSettings.updateEquipmentOnPlayerActions) this.player.detectEquipmentUpdates(); // Paper - Force update attributes.
+            // MultiPaper start - Sync player
+            if (puregero.multipaper.MultiPaper.isRealPlayer(this.player)) {
+                puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(
+                        this.player, new puregero.multipaper.externalserverprotocol.PlayerActionPacket(this.player, packet)
+                );
+            } else {
+                this.player.detectEquipmentUpdates(); // Paper - Force update attributes.
+            }
+            // MultiPaper end - Sync player
         } else {
             LOGGER.warn("{} tried to set an invalid carried item", this.player.getPlainTextName());
             this.disconnect(Component.literal("Invalid hotbar selection (Hacking?)"), org.bukkit.event.player.PlayerKickEvent.Cause.ILLEGAL_ACTION); // CraftBukkit // Paper - kick event cause
@@ -2850,6 +2880,12 @@ public class ServerGamePacketListenerImpl
             return;
         }
         // CraftBukkit end
+        // MultiPaper start - Sync player
+        if (this.player instanceof puregero.multipaper.ExternalPlayer) {
+            puregero.multipaper.externalserverprotocol.PlayerChatMessagePacket.broadcastChatMessage(message, chatType, this.player.getUUID());
+            return;
+        }
+        // MultiPaper end - Sync player
         // Paper start - Ensure that client receives chat packets in the same order that we add into the message signature cache
         synchronized (this.messageSignatureCache) {
         this.send(
@@ -2926,6 +2962,7 @@ public class ServerGamePacketListenerImpl
             }
             // Spigot end
             this.player.resetLastActionTime();
+            if (target != null && puregero.multipaper.MultiPaperEntityInteractHandler.handleEntityAttack(this.player, target, packet)) return; // MultiPaper - Sync player
             if (target != null && level.getWorldBorder().isWithinBounds(target.blockPosition())) {
                 AABB targetBounds = target.getBoundingBox();
                 ItemStack mainHandItem = this.player.getMainHandItem();
@@ -2970,6 +3007,7 @@ public class ServerGamePacketListenerImpl
             // Spigot end
             this.player.resetLastActionTime();
             this.player.setShiftKeyDown(packet.usingSecondaryAction());
+            if (target != null && puregero.multipaper.MultiPaperEntityInteractHandler.handleEntityInteract(this.player, target, packet)) return; // MultiPaper - Sync player
             if (target != null && level.getWorldBorder().isWithinBounds(target.blockPosition())) {
                 AABB targetBounds = target.getBoundingBox();
                 if (this.player.isWithinEntityInteractionRange(targetBounds, io.papermc.paper.configuration.GlobalConfiguration.get().misc.clientInteractionLeniencyDistance.or(3.0))) { // Paper - configurable lenience value for interact range
@@ -3126,6 +3164,7 @@ public class ServerGamePacketListenerImpl
     public void handleContainerClose(ServerboundContainerClosePacket packet, org.bukkit.event.inventory.InventoryCloseEvent.Reason reason) {
         // Paper end - Inventory close reason
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet)) return; // MultiPaper - Sync player
 
         if (this.player.isImmobile()) return; // CraftBukkit
         CraftEventFactory.handleInventoryCloseEvent(this.player, reason); // CraftBukkit // Paper
@@ -3135,6 +3174,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleContainerClick(final ServerboundContainerClickPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet)) return; // MultiPaper - Sync player
         if (this.player.isImmobile()) return; // CraftBukkit
         this.player.resetLastActionTime();
         if (this.player.containerMenu.containerId == packet.containerId() && this.player.containerMenu.stillValid(this.player)) { // CraftBukkit
@@ -3477,6 +3517,7 @@ public class ServerGamePacketListenerImpl
         }
         // Paper end - auto recipe limit
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet)) return; // MultiPaper - Sync player
         this.player.resetLastActionTime();
         if (!this.player.isSpectator() && this.player.containerMenu.containerId == packet.containerId()) {
             if (!this.player.containerMenu.stillValid(this.player)) {
@@ -3543,6 +3584,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handleContainerButtonClick(final ServerboundContainerButtonClickPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        if (puregero.multipaper.MultiPaperInventoryHandler.handleContainerEvent(this.player, packet)) return; // MultiPaper - Sync player
         if (this.player.isImmobile()) return; // CraftBukkit
         this.player.resetLastActionTime();
         if (this.player.containerMenu.containerId == packet.containerId() && !this.player.isSpectator()) {
@@ -3625,6 +3667,7 @@ public class ServerGamePacketListenerImpl
 
     @Override
     public void handleSignUpdate(final ServerboundSignUpdatePacket packet) {
+        if (puregero.multipaper.MultiPaperSignHandler.handleSignUpdate(this.player, packet)) return; // MultiPaper - Sync player
         // Paper start - Limit client sign length
         String[] linesArray = packet.getLines();
         for (int i = 0; i < linesArray.length; ++i) {
@@ -3668,6 +3711,7 @@ public class ServerGamePacketListenerImpl
     @Override
     public void handlePlayerAbilities(final ServerboundPlayerAbilitiesPacket packet) {
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
+        puregero.multipaper.MultiPaperPlayerHandler.handlePlayerAbilities(this.player, packet); // MultiPaper - Sync player
         // CraftBukkit start
         if (this.player.getAbilities().mayfly && this.player.getAbilities().flying != packet.isFlying()) {
             PlayerToggleFlightEvent event = new PlayerToggleFlightEvent(this.player.getBukkitEntity(), packet.isFlying());
@@ -3691,9 +3735,10 @@ public class ServerGamePacketListenerImpl
             return;
         }
         // Paper end - do not accept invalid information
+        puregero.multipaper.MultiPaperPlayerHandler.handleClientInformation(this.player, packet); // MultiPaper - Sync player
         boolean wasHatShown = this.player.isModelPartShown(PlayerModelPart.HAT);
         this.player.updateOptions(packet.information());
-        this.connection.channel.attr(io.papermc.paper.adventure.PaperAdventure.LOCALE_ATTRIBUTE).set(net.kyori.adventure.translation.Translator.parseLocale(packet.information().language())); // Paper
+        if (!(this.player instanceof puregero.multipaper.ExternalPlayer)) this.connection.channel.attr(io.papermc.paper.adventure.PaperAdventure.LOCALE_ATTRIBUTE).set(net.kyori.adventure.translation.Translator.parseLocale(packet.information().language())); // Paper // MultiPaper - Sync player
         if (this.player.isModelPartShown(PlayerModelPart.HAT) != wasHatShown) {
             this.server.getPlayerList().broadcastAll(new ClientboundPlayerInfoUpdatePacket(ClientboundPlayerInfoUpdatePacket.Action.UPDATE_HAT, this.player));
         }
diff --git a/net/minecraft/server/players/IpBanList.java b/net/minecraft/server/players/IpBanList.java
index b91cc278d1f76032d37ea69500577ce9fb63c19f..f760fb4c6ea30d8819eee91029ebc3ab840d6305 100644
--- a/net/minecraft/server/players/IpBanList.java
+++ b/net/minecraft/server/players/IpBanList.java
@@ -45,6 +45,7 @@ public class IpBanList extends StoredUserList<String, IpBanListEntry> {
 
     @Override
     public boolean add(final IpBanListEntry infos) {
+        puregero.multipaper.externalserverprotocol.BanIpPacket.broadcast(infos.getUser(), infos.created, infos.expires, infos.reason, infos.source); // MultiPaper - Sync player
         if (super.add(infos)) {
             if (infos.getUser() != null) {
                 this.notificationService.ipBanned(infos);
@@ -58,6 +59,7 @@ public class IpBanList extends StoredUserList<String, IpBanListEntry> {
 
     @Override
     public boolean remove(final String ip) {
+        puregero.multipaper.externalserverprotocol.PardonIpPacket.broadcast(ip); // MultiPaper - Sync player
         if (super.remove(ip)) {
             this.notificationService.ipUnbanned(ip);
             return true;
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index 331c7815552f2aa626eb5a1386d1769c4fad063f..11a752e07f8453e353c92516f6d5d32c968329e4 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -114,6 +114,7 @@ public abstract class PlayerList {
     private static final SimpleDateFormat BAN_DATE_FORMAT = new SimpleDateFormat("yyyy-MM-dd 'at' HH:mm:ss z", Locale.ROOT);
     private final MinecraftServer server;
     private final List<ServerPlayer> players = new java.util.concurrent.CopyOnWriteArrayList(); // CraftBukkit - ArrayList -> CopyOnWriteArrayList: Iterator safety
+    private final List<ServerPlayer> localPlayers = new java.util.concurrent.CopyOnWriteArrayList(); // MultiPaper - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
     private final Map<UUID, ServerPlayer> playersByUUID = Maps.newHashMap();
     private final UserBanList bans;
     private final IpBanList ipBans;
@@ -162,6 +163,19 @@ public abstract class PlayerList {
         UserNameToIdResolver profileCache = this.server.services().nameToIdCache();
         Optional<NameAndId> oldProfile = profileCache.get(gameProfile.id());
         String oldName = oldProfile.map(NameAndId::name).orElse(gameProfile.name());
+        // MultiPaper start - Sync player
+        if (!puregero.multipaper.MultiPaper.sendPlayerConnect(player).join()) {
+            Component reason = Component.literal("You are already logged into another server.");
+            connection.send(new net.minecraft.network.protocol.common.ClientboundDisconnectPacket(reason));
+            connection.disconnect(reason);
+            return;
+        }
+        player.hasRegisteredOnMaster = true;
+        if (!connection.isConnected()) {
+            puregero.multipaper.MultiPaper.sendPlayerDisconnect(player);
+            return;
+        }
+        // MultiPaper end - Sync player
         if (player.lastKnownName != null) { oldName = player.lastKnownName; player.lastKnownName = null; } // CraftBukkit - Better rename detection
         profileCache.add(gameProfile);
         ServerLevel level = player.level();
@@ -223,6 +237,7 @@ public abstract class PlayerList {
 
         // player.connection.send(ClientboundPlayerInfoUpdatePacket.createPlayerInitializing(this.players)); // CraftBukkit - replaced with loop below
         this.players.add(player);
+        if (!(player instanceof puregero.multipaper.ExternalPlayer)) this.localPlayers.add(player); // MultiPaper - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
         this.playersByName.put(player.getScoreboardName().toLowerCase(java.util.Locale.ROOT), player); // Spigot
         this.playersByUUID.put(player.getUUID(), player);
         // this.broadcastAll(ClientboundPlayerInfoUpdatePacket.createPlayerInitializing(List.of(player))); // CraftBukkit - replaced with loop below
@@ -254,6 +269,8 @@ public abstract class PlayerList {
         }
         // CraftBukkit end
 
+        puregero.multipaper.MultiPaper.onPlayerJoin(player); // MultiPaper - Sync player
+
         // CraftBukkit start - sendAll above replaced with this loop
         ClientboundPlayerInfoUpdatePacket packet = ClientboundPlayerInfoUpdatePacket.createPlayerInitializing(List.of(player)); // Paper - Add Listing API for Player
 
@@ -415,6 +432,7 @@ public abstract class PlayerList {
 
     protected void save(final ServerPlayer player) {
         if (!player.getBukkitEntity().isPersistent()) return; // CraftBukkit
+        if (puregero.multipaper.MultiPaper.isExternalPlayer(player)) return; // MultiPaper - Sync player
         player.lastSave = MinecraftServer.currentTick; // Paper - Incremental chunk and player saving
         this.playerIo.save(player);
         ServerStatsCounter stats = player.getStats(); // CraftBukkit
@@ -444,7 +462,7 @@ public abstract class PlayerList {
         }
 
         org.bukkit.event.player.PlayerQuitEvent playerQuitEvent = new org.bukkit.event.player.PlayerQuitEvent(player.getBukkitEntity(), leaveMessage, player.quitReason); // Paper - Adventure & Add API for quit reason
-        this.cserver.getPluginManager().callEvent(playerQuitEvent);
+        if (puregero.multipaper.MultiPaper.isRealPlayer(player)) this.cserver.getPluginManager().callEvent(playerQuitEvent); // MultiPaper - Sync player - don't run for external players
         player.getBukkitEntity().disconnect();
 
         // CraftBukkit end
@@ -500,6 +518,7 @@ public abstract class PlayerList {
         player.retireScheduler(); // Paper - Folia schedulers
         player.getAdvancements().clearTriggers();
         this.players.remove(player);
+        if (!(player instanceof puregero.multipaper.ExternalPlayer)) this.localPlayers.remove(player); // MultiPaper - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
         this.playersByName.remove(player.getScoreboardName().toLowerCase(java.util.Locale.ROOT)); // Spigot
         this.server.getCustomBossEvents().onPlayerDisconnect(player);
         UUID uuid = player.getUUID();
@@ -528,6 +547,7 @@ public abstract class PlayerList {
         // This removes the scoreboard (and player reference) for the specific player in the manager
         this.cserver.getScoreboardManager().removePlayer(player.getBukkitEntity());
         // CraftBukkit end
+        puregero.multipaper.MultiPaper.onPlayerDisconnect(player); // MultiPaper - Sync player
         return playerQuitEvent.quitMessage(); // Paper - Adventure
     }
 
@@ -596,14 +616,23 @@ public abstract class PlayerList {
 
     // Paper start - respawn event
     public ServerPlayer respawn(final ServerPlayer serverPlayer, final boolean keepAllPlayerData, final Entity.RemovalReason removalReason, final org.bukkit.event.player.PlayerRespawnEvent.RespawnReason respawnReason) {
+        // MultiPaper start - Sync player
+        if (puregero.multipaper.MultiPaper.isExternalPlayer(serverPlayer)) {
+            ((puregero.multipaper.ExternalPlayer) serverPlayer).externalServerConnection.send(
+                    new puregero.multipaper.externalserverprotocol.PlayerRespawnPacket(serverPlayer.getUUID(), keepAllPlayerData)
+            );
+            return serverPlayer;
+        }
+        // MultiPaper end - Sync playe
         ServerPlayer.RespawnResult result = serverPlayer.findRespawnPositionAndUseSpawnBlock0(!keepAllPlayerData, TeleportTransition.DO_NOTHING, respawnReason);
         if (result == null) { // disconnected player during the respawn event
             return serverPlayer;
         }
         TeleportTransition respawnInfo = result.transition();
-        Level fromLevel = serverPlayer.level();
+        ServerLevel fromLevel = serverPlayer.level(); // MultiPaper - Sync player
         // Paper end - respawn event
         this.players.remove(serverPlayer);
+        if (!(serverPlayer instanceof puregero.multipaper.ExternalPlayer)) this.localPlayers.remove(serverPlayer); // MultiPaper - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
         this.playersByName.remove(serverPlayer.getScoreboardName().toLowerCase(java.util.Locale.ROOT)); // Paper
         serverPlayer.level().removePlayerImmediately(serverPlayer, removalReason);
         ServerLevel level = respawnInfo.newLevel();
@@ -648,6 +677,7 @@ public abstract class PlayerList {
         this.sendPlayerPermissionLevel(player);
         level.addRespawnedPlayer(player);
         this.players.add(player);
+        if (!(player instanceof puregero.multipaper.ExternalPlayer)) this.localPlayers.add(player); // MultiPaper - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
         this.playersByName.put(player.getScoreboardName().toLowerCase(java.util.Locale.ROOT), player); // Paper
         this.playersByUUID.put(player.getUUID(), player);
         player.initInventoryMenu();
@@ -705,6 +735,26 @@ public abstract class PlayerList {
         ).callEvent();
         // Paper end
 
+        // MultiPaper start - Sync player
+        List<puregero.multipaper.ExternalServerConnection> serversToCreatePlayerOn = new java.util.ArrayList<>();
+        puregero.multipaper.MultiPaper.forEachExternalServer(externalServer -> {
+            if (externalServer.getConnection() != null && externalServer.getConnection().isOpen()) {
+                boolean subscribedToSoruce = externalServer.getConnection().subscribedWorlds.contains(fromLevel.uuid);
+                boolean subscribedToDestination = externalServer.getConnection().subscribedWorlds.contains(level.uuid);
+
+                if (subscribedToSoruce && subscribedToDestination) {
+                    externalServer.getConnection().send(new puregero.multipaper.externalserverprotocol.PlayerChangeDimensionPacket(serverPlayer, !keepAllPlayerData));
+                    externalServer.getConnection().send(new puregero.multipaper.externalserverprotocol.EntityUpdatePacket(serverPlayer, new net.minecraft.network.protocol.game.ClientboundSetEntityDataPacket(serverPlayer.getId(), serverPlayer.getEntityData().packAll())));
+                } else if (subscribedToDestination) {
+                    serversToCreatePlayerOn.add(externalServer.getConnection());
+                } else if (subscribedToSoruce) {
+                    externalServer.getConnection().send(new puregero.multipaper.externalserverprotocol.PlayerRemovePacket(serverPlayer.getUUID()));
+                }
+            }
+        });
+        puregero.multipaper.externalserverprotocol.PlayerCreatePacket.sendPlayer(serverPlayer, serversToCreatePlayerOn.toArray(puregero.multipaper.ExternalServerConnection[]::new));
+        // MultiPaper end - Sync player
+
         return player;
     }
 
@@ -734,13 +784,27 @@ public abstract class PlayerList {
         this.sendPlayerPermissionLevel(player, permissions, recalculatePermissions); // Paper - avoid recalculating permissions if possible
     }
 
+    // MultiPaper start - Sync player
+    public void addPlayer(ServerPlayer player) {
+        players.add(player);
+        playersByUUID.put(player.getUUID(), player);
+        playersByName.put(player.getScoreboardName().toLowerCase(), player);
+    }
+
+    public void removePlayer(ServerPlayer player) {
+        players.remove(player);
+        playersByUUID.remove(player.getUUID());
+        playersByName.remove(player.getScoreboardName().toLowerCase());
+    }
+    // MultiPaper end - Sync player
+
     public void tick() {
         if (++this.sendAllPlayerInfoIn > 600) {
             // CraftBukkit start
             for (int i = 0; i < this.players.size(); ++i) {
                 final ServerPlayer target = this.players.get(i);
 
-                target.connection.send(new ClientboundPlayerInfoUpdatePacket(EnumSet.of(ClientboundPlayerInfoUpdatePacket.Action.UPDATE_LATENCY), com.google.common.collect.Collections2.filter(this.players, t -> target.getBukkitEntity().canSee(t.getBukkitEntity()))));
+                target.connection.send(new ClientboundPlayerInfoUpdatePacket(EnumSet.of(ClientboundPlayerInfoUpdatePacket.Action.UPDATE_LATENCY), com.google.common.collect.Collections2.filter(this.localPlayers, t -> target.getBukkitEntity().canSee(t.getBukkitEntity())))); // MultiPaper - Sync player - use this.localPlayers instead of this.players
             }
             // CraftBukkit end
             this.sendAllPlayerInfoIn = 0;
@@ -911,7 +975,7 @@ public abstract class PlayerList {
 
     // Paper start - whitelist verify event / login event
     public LoginResult canBypassFullServerLogin(final NameAndId nameAndId, final LoginResult currentResult) {
-        final boolean shouldKick = this.players.size() >= this.getMaxPlayers() && !(/*player.hasPermission("purpur.joinfullserver") || */this.canBypassPlayerLimit(nameAndId)); // Purpur - Allow player join full server by permission TODO: this hasn't worked for a while, so comment it out until we can reliably check perms of the player joining
+        final boolean shouldKick = (puregero.multipaper.config.MultiPaperConfiguration.get().syncSettings.useLocalPlayerCountForServerIsFullKick ? this.localPlayers.size() >= this.getMaxPlayers() : this.players.size() >= this.getMaxPlayers()) && !(/*player.hasPermission("purpur.joinfullserver") || */this.canBypassPlayerLimit(nameAndId)); // Purpur - Allow player join full server by permission TODO: this hasn't worked for a while, so comment it out until we can reliably check perms of the player joining
         final io.papermc.paper.event.player.PlayerServerFullCheckEvent fullCheckEvent = new io.papermc.paper.event.player.PlayerServerFullCheckEvent(
             new com.destroystokyo.paper.profile.CraftPlayerProfile(nameAndId),
             io.papermc.paper.adventure.PaperAdventure.asAdventure(currentResult.message),
@@ -998,7 +1062,7 @@ public abstract class PlayerList {
         // Paper start - Incremental chunk and player saving
             int numSaved = 0;
             final long now = MinecraftServer.currentTick;
-            for (final ServerPlayer player : this.players) {
+            for (final ServerPlayer player : this.localPlayers) { // MultiPaper - Sync player
             if (interval == -1 || now - player.lastSave >= interval) {
                 this.save(player);
                 if (interval != -1 && ++numSaved >= io.papermc.paper.configuration.GlobalConfiguration.get().playerAutoSave.maxPerTick()) {
@@ -1211,7 +1275,7 @@ public abstract class PlayerList {
     }
     public ServerStatsCounter getPlayerStats(final GameProfile gameProfile) {
             Path targetFile = this.locateStatsFile(gameProfile);
-            return new ServerStatsCounter(this.server, targetFile);
+            return new ServerStatsCounter(this.server, targetFile, gameProfile.id()); // MultiPaper - Sync player
     }
     // CraftBukkit end
 
@@ -1273,6 +1337,12 @@ public abstract class PlayerList {
         return this.players;
     }
 
+    // MultiPaper start - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
+    public List<ServerPlayer> getLocalPlayers() {
+        return this.localPlayers;
+    }
+    // MultiPaper end - Sync player - add getAllOnlinePlayers and getLocalOnlinePlayers
+
     public Map<UUID, ServerPlayer> getPlayersByUUID() {
         return this.playersByUUID;
     }
diff --git a/net/minecraft/server/players/ServerOpList.java b/net/minecraft/server/players/ServerOpList.java
index daf5f663517c7e24eadabff5fbed543be2f952c2..2f127632925a03191155d2ef61de1445e4aa9f9a 100644
--- a/net/minecraft/server/players/ServerOpList.java
+++ b/net/minecraft/server/players/ServerOpList.java
@@ -22,6 +22,7 @@ public class ServerOpList extends StoredUserList<NameAndId, ServerOpListEntry> {
 
     @Override
     public boolean add(final ServerOpListEntry infos) {
+        puregero.multipaper.externalserverprotocol.OpPlayerPacket.broadcast(infos.getUser().name(), infos.getUser().id(), infos.permissions().level().id(), infos.getBypassesPlayerLimit()); // MultiPaper - Sync player
         if (super.add(infos)) {
             if (infos.getUser() != null) {
                 this.notificationService.playerOped(infos);
@@ -35,6 +36,7 @@ public class ServerOpList extends StoredUserList<NameAndId, ServerOpListEntry> {
 
     @Override
     public boolean remove(final NameAndId user) {
+        puregero.multipaper.externalserverprotocol.DeopPlayerPacket.broadcast(user.name(), user.id()); // MultiPaper - Sync player
         ServerOpListEntry entry = this.get(user);
         if (super.remove(user)) {
             if (entry != null) {
diff --git a/net/minecraft/server/players/StoredUserList.java b/net/minecraft/server/players/StoredUserList.java
index eaf8fb8a9cf94d0d16debf90e5ceda3f91c979ef..608ad379f6e36bede7e749408c08be511c117590 100644
--- a/net/minecraft/server/players/StoredUserList.java
+++ b/net/minecraft/server/players/StoredUserList.java
@@ -129,16 +129,22 @@ public abstract class StoredUserList<K, V extends StoredUserEntry<K>> {
         JsonArray result = new JsonArray();
         this.map.values().stream().map(entry -> Util.make(new JsonObject(), entry::serialize)).forEach(result::add);
 
-        try (BufferedWriter writer = Files.newWriter(this.file, StandardCharsets.UTF_8)) {
-            GSON.toJson(result, GSON.newJsonWriter(writer));
-        }
+        puregero.multipaper.MultiPaper.writeJson(this.file.getName(), GSON.toJson(result)); // MultiPaper - Sync player
     }
 
     public void load() throws IOException {
-        if (this.file.exists()) {
-            try (BufferedReader reader = Files.newReader(this.file, StandardCharsets.UTF_8)) {
+        // MultiPaper start - Sync player
+        if (true) {
+            try {
+        // MultiPaper end - Sync player
                 this.map.clear();
-                JsonArray contents = GSON.fromJson(reader, JsonArray.class);
+                // MultiPaper start - Sync player
+                String json = puregero.multipaper.MultiPaper.readJson(this.file.getName());
+                if (json == null || json.isEmpty()) {
+                    return;
+                }
+                JsonArray contents = GSON.fromJson(json, JsonArray.class);
+                // MultiPaper end - Sync player
                 if (contents == null) {
                     return;
                 }
diff --git a/net/minecraft/server/players/UserBanList.java b/net/minecraft/server/players/UserBanList.java
index 35fa92e3db1bf79b2fd89f21e28a9c2470c6c5a6..499a96f7d745221c54b58afa098f597b1add161f 100644
--- a/net/minecraft/server/players/UserBanList.java
+++ b/net/minecraft/server/players/UserBanList.java
@@ -31,6 +31,7 @@ public class UserBanList extends StoredUserList<NameAndId, UserBanListEntry> {
 
     @Override
     public boolean add(final UserBanListEntry infos) {
+        puregero.multipaper.externalserverprotocol.BanPlayerPacket.broadcast(infos.getUser().name(), infos.getUser().id(), infos.created, infos.expires, infos.reason, infos.source); // MultiPaper - Sync player
         if (super.add(infos)) {
             if (infos.getUser() != null) {
                 this.notificationService.playerBanned(infos);
@@ -44,6 +45,7 @@ public class UserBanList extends StoredUserList<NameAndId, UserBanListEntry> {
 
     @Override
     public boolean remove(final NameAndId user) {
+        puregero.multipaper.externalserverprotocol.PardonPlayerPacket.broadcast(user.name(), user.id()); // MultiPaper - Sync player
         if (super.remove(user)) {
             this.notificationService.playerUnbanned(user);
             return true;
diff --git a/net/minecraft/server/players/UserWhiteList.java b/net/minecraft/server/players/UserWhiteList.java
index e42147dca81ef47e15e7407b6136a7121cac3c64..94451b7a32fb3e63b8be1effb093635477a74ed3 100644
--- a/net/minecraft/server/players/UserWhiteList.java
+++ b/net/minecraft/server/players/UserWhiteList.java
@@ -21,6 +21,7 @@ public class UserWhiteList extends StoredUserList<NameAndId, UserWhiteListEntry>
 
     @Override
     public boolean add(final UserWhiteListEntry infos) {
+        puregero.multipaper.externalserverprotocol.WhiteListPlayerPacket.broadcast(infos.getUser().name(), infos.getUser().id()); // MultiPaper - Sync player
         // Paper start - Add whitelist events
         if (!new io.papermc.paper.event.server.WhitelistStateUpdateEvent(com.destroystokyo.paper.profile.CraftPlayerProfile.asBukkitCopy(infos.getUser().toUncompletedGameProfile()), io.papermc.paper.event.server.WhitelistStateUpdateEvent.WhitelistStatus.ADDED).callEvent()) {
             return false;
@@ -39,6 +40,7 @@ public class UserWhiteList extends StoredUserList<NameAndId, UserWhiteListEntry>
 
     @Override
     public boolean remove(final NameAndId user) {
+        puregero.multipaper.externalserverprotocol.RemoveWhiteListedPlayerPacket.broadcast(user.name(), user.id()); // MultiPaper - Sync player
         // Paper start - Add whitelist events
         if (!new io.papermc.paper.event.server.WhitelistStateUpdateEvent(com.destroystokyo.paper.profile.CraftPlayerProfile.asBukkitCopy(user.toUncompletedGameProfile()), io.papermc.paper.event.server.WhitelistStateUpdateEvent.WhitelistStatus.REMOVED).callEvent()) {
             return false;
diff --git a/net/minecraft/stats/ServerRecipeBook.java b/net/minecraft/stats/ServerRecipeBook.java
index 9a278c857c2a6e40c2a3d2a2991ab8c7676da4c6..8d1b699d81955715511812d43de26231651bf470 100644
--- a/net/minecraft/stats/ServerRecipeBook.java
+++ b/net/minecraft/stats/ServerRecipeBook.java
@@ -42,6 +42,7 @@ public class ServerRecipeBook extends RecipeBook {
     }
 
     public boolean contains(final ResourceKey<Recipe<?>> id) {
+        if (id != null) return true; // MultiPaper - Sync player - who cares if they use a recipe that they don't have, no point trying to sync this between servers
         return this.known.contains(id);
     }
 
diff --git a/net/minecraft/stats/ServerStatsCounter.java b/net/minecraft/stats/ServerStatsCounter.java
index f5beb001b938072e78c0ead7e0ad0499784ed1cf..8b9b3381bec9880e5c3f9b42b0c0f9821bb1f643 100644
--- a/net/minecraft/stats/ServerStatsCounter.java
+++ b/net/minecraft/stats/ServerStatsCounter.java
@@ -63,10 +63,14 @@ public class ServerStatsCounter extends StatsCounter {
         return Codec.unboundedMap(statCodec, Codec.INT);
     }
 
-    public ServerStatsCounter(final MinecraftServer server, final Path file) {
+    public ServerStatsCounter(final MinecraftServer server, final Path file, final java.util.UUID uuid) { // MultiPaper - Sync player
         this.file = file;
-        if (Files.isRegularFile(file)) {
-            try (Reader reader = Files.newBufferedReader(file, StandardCharsets.UTF_8)) {
+        // MultiPaper start - Sync player
+        byte[] bytes = puregero.multipaper.ExternalPlayer.loadedStats.remove(uuid);
+        if (bytes == null) bytes = puregero.multipaper.MultiPaper.readFile(file.toString());
+        if (bytes != null && bytes.length != 0) {
+            try (Reader reader = new java.io.InputStreamReader(new java.io.ByteArrayInputStream(bytes), StandardCharsets.UTF_8)) {
+        // MultiPaper end - Sync player
                 JsonElement element = StrictJsonParser.parse(reader);
                 this.parse(server.getFixerUpper(), element);
             } catch (IOException e) {
@@ -90,6 +94,12 @@ public class ServerStatsCounter extends StatsCounter {
 
     public void save() {
         if (org.spigotmc.SpigotConfig.disableStatSaving) return; // Spigot
+        // MultiPaper start - Sync player
+        if (true) {
+            puregero.multipaper.MultiPaper.writeFile(this.file.toString(), GSON.toJson(this.toJson()).getBytes(StandardCharsets.UTF_8));
+            return;
+        }
+        // MultiPaper end - Sync player
         try {
             FileUtil.createDirectoriesSafe(this.file.getParent());
 
@@ -126,7 +136,7 @@ public class ServerStatsCounter extends StatsCounter {
             );
     }
 
-    protected JsonElement toJson() {
+    public JsonElement toJson() { // MultiPaper - Sync player - make public
         JsonObject result = new JsonObject();
         result.add("stats", STATS_CODEC.encodeStart(JsonOps.INSTANCE, this.stats).getOrThrow());
         result.addProperty("DataVersion", SharedConstants.getCurrentVersion().dataVersion().version());
diff --git a/net/minecraft/stats/StatsCounter.java b/net/minecraft/stats/StatsCounter.java
index d617739becbad0611974138a2f8f9ff0e17d05c0..ef1ae43b3e8d048ed66b1d89c9f2013a02d50866 100644
--- a/net/minecraft/stats/StatsCounter.java
+++ b/net/minecraft/stats/StatsCounter.java
@@ -20,6 +20,7 @@ public class StatsCounter {
             return;
         }
         // CraftBukkit end
+        puregero.multipaper.MultiPaperStatHandler.onStatIncrease(player.getUUID(), stat, count); // MultiPaper - Sync player
         this.setValue(player, stat, result);
     }
 
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index b22d6dad30c5a79bc6ff3968034b62974e1fe3c8..2ccc15e2294b1b0475ae0574c637544279c28095 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -1881,7 +1881,26 @@ public abstract class Entity
                 }
 
                 iterations.set(iteration);
-                BlockState state = this.level().getBlockState(blockIntersection);
+                // MultiPaper start - Sync player
+                // only check for blocks on our server (or portals)
+                // lazy load - load blockState here only if we'll use it for the check
+                BlockState state = this instanceof ServerPlayer ? this.level().getBlockState(blockIntersection) : null;
+                boolean tickOnLocalServer = state != null &&
+                        (state.getBlock() instanceof net.minecraft.world.level.block.WebBlock ||
+                                state.getBlock() instanceof net.minecraft.world.level.block.EndPortalBlock ||
+                                state.getBlock() instanceof net.minecraft.world.level.block.PowderSnowBlock ||
+                                state.getBlock() instanceof net.minecraft.world.level.block.NetherPortalBlock ||
+                                state.getBlock() instanceof net.minecraft.world.level.block.SweetBerryBushBlock
+                        );
+                if (!(tickOnLocalServer && puregero.multipaper.MultiPaper.isRealPlayer(this)) && puregero.multipaper.MultiPaper.isChunkExternal(this.level.getChunkAt(blockIntersection))){
+                    return false;
+                }
+                if (tickOnLocalServer && puregero.multipaper.MultiPaper.isExternalPlayer(this)) {
+                    return false;
+                }
+                // lazy load - fully load blockState here since we passed the check and will use it now
+                state = state != null ? state : this.level().getBlockState(blockIntersection);
+                // MultiPaper end - Sync player
                 if (state.isAir()) {
                     if (debugEntityBlockIntersections) {
                         this.debugBlockIntersection((ServerLevel)this.level(), blockIntersection.immutable(), false, false);
@@ -3413,6 +3432,14 @@ public abstract class Entity
                     .forEach(player -> CriteriaTriggers.START_RIDING_TRIGGER.trigger((ServerPlayer)player));
             }
 
+            // MultiPaper start - Sync player
+            // inform the player's server that they are now riding an entity and hand them the entity's data
+            Entity rootVehicle = this.getRootVehicle();
+            if (puregero.multipaper.MultiPaperEntitiesHandler.getControllingPassenger(rootVehicle) == this && puregero.multipaper.MultiPaper.isChunkLocal(level.getChunkIfLoaded(rootVehicle.blockPosition)) && this instanceof puregero.multipaper.ExternalPlayer player) {
+                puregero.multipaper.externalserverprotocol.EntityUpdateWithDependenciesPacket.sendVehicleAndPassengersPacketsRecursivelyToServers(rootVehicle, List.of(player.externalServerConnection.externalServer));
+            }
+            // MultiPaper end - Sync player
+
             return true;
         } else {
             return false;
@@ -3437,6 +3464,7 @@ public abstract class Entity
     public void removeVehicle(boolean suppressCancellation) {
         // Paper end - Force entity dismount during teleportation
         if (this.vehicle != null) {
+            boolean wasControllingPassenger = puregero.multipaper.MultiPaperEntitiesHandler.getControllingPassenger(this.vehicle) == this; // MultiPaper - Sync player
             Entity oldVehicle = this.vehicle;
             this.vehicle = null;
             if (!oldVehicle.removePassenger(this, suppressCancellation)) this.vehicle = oldVehicle; // CraftBukkit // Paper - Force entity dismount during teleportation
@@ -3444,6 +3472,21 @@ public abstract class Entity
             if (removalReason == null || removalReason.shouldDestroy()) {
                 this.level().gameEvent(this, GameEvent.ENTITY_DISMOUNT, oldVehicle.position);
             }
+            // MultiPaper start - Sync player
+            // inform other servers that the player is no longer riding the entity and hand them the entity's data
+            ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder newChunkHolder = puregero.multipaper.MultiPaper.getChunkHolder(oldVehicle);
+            if (this.vehicle == null && puregero.multipaper.MultiPaper.isRealPlayer(this) && wasControllingPassenger && !puregero.multipaper.externalserverprotocol.EntityRemovePacket.removedEntities.containsKey(oldVehicle.getRootVehicle().getUUID())) {
+                // any server might now be controlling this vehicle
+                Set<puregero.multipaper.ExternalServer> servers = new java.util.LinkedHashSet<>(newChunkHolder == null ? java.util.Collections.emptyList() : newChunkHolder.externalEntitiesSubscribers);
+
+                if (puregero.multipaper.MultiPaperEntitiesHandler.getControllingPassenger(oldVehicle.getRootVehicle()) instanceof puregero.multipaper.ExternalPlayer newController) {
+                    // make sure we send the vehicle to the new controlling passenger, even if they don't have the chunk loaded
+                    servers.add(newController.externalServerConnection.externalServer);
+                }
+
+                puregero.multipaper.externalserverprotocol.EntityUpdateWithDependenciesPacket.sendVehicleAndPassengersPacketsRecursivelyToServers(oldVehicle.getRootVehicle(), servers);
+            }
+            // MultiPaper end - Sync player
         }
     }
 
@@ -3535,6 +3578,12 @@ public abstract class Entity
             this.passengers = this.passengers.stream().filter(p -> p != passenger).collect(ImmutableList.toImmutableList());
         }
 
+        // MultiPaper start - Sync player
+        if (passenger instanceof ServerPlayer player) {
+            player.closeContainer();
+        }
+        // MultiPaper end - Sync player
+
         passenger.boardingCooldown = 60;
         return true; // CraftBukkit
     }
diff --git a/net/minecraft/world/entity/EntityEquipment.java b/net/minecraft/world/entity/EntityEquipment.java
index 20041325ae77f16a5b77f5de7255788b52ebb3a5..1def27d3ecd6e218312b4fe00fdb8e286d7802c1 100644
--- a/net/minecraft/world/entity/EntityEquipment.java
+++ b/net/minecraft/world/entity/EntityEquipment.java
@@ -28,7 +28,13 @@ public class EntityEquipment {
     }
 
     public ItemStack set(final EquipmentSlot slot, final ItemStack itemStack) {
-        return Objects.requireNonNullElse(this.items.put(slot, itemStack), ItemStack.EMPTY);
+        // MultiPaper start - Sync player
+        ItemStack old = Objects.requireNonNullElse(this.items.put(slot, itemStack), ItemStack.EMPTY);
+        if (this instanceof net.minecraft.world.entity.player.PlayerEquipment pe && pe.player instanceof net.minecraft.server.level.ServerPlayer p) {
+            puregero.multipaper.MultiPaperInventoryHandler.broadcastEquipmentChanges(p, Map.of(slot, old), Map.of(slot, itemStack));
+        }
+        return old;
+        // MultiPaper end - Sync player
     }
 
     public ItemStack get(final EquipmentSlot slot) {
@@ -55,8 +61,14 @@ public class EntityEquipment {
     }
 
     public void setAll(final EntityEquipment equipment) {
+        EnumMap<EquipmentSlot, ItemStack> olds = new EnumMap<>(this.items); // MultiPaper - Sync player
         this.items.clear();
         this.items.putAll(equipment.items);
+        // MultiPaper start - Sync player
+        if (this instanceof net.minecraft.world.entity.player.PlayerEquipment pe && pe.player instanceof net.minecraft.server.level.ServerPlayer p) {
+            puregero.multipaper.MultiPaperInventoryHandler.broadcastEquipmentChanges(p, olds, this.items);
+        }
+        // MultiPaper end - Sync player
     }
 
     public void dropAll(final LivingEntity dropper) {
@@ -68,7 +80,13 @@ public class EntityEquipment {
     }
 
     public void clear() {
+        EnumMap<EquipmentSlot, ItemStack> olds = new EnumMap<>(this.items); // MultiPaper - Sync player
         this.items.replaceAll((s, v) -> ItemStack.EMPTY);
+        // MultiPaper start - Sync player
+        if (this instanceof net.minecraft.world.entity.player.PlayerEquipment pe && pe.player instanceof net.minecraft.server.level.ServerPlayer p) {
+            puregero.multipaper.MultiPaperInventoryHandler.broadcastEquipmentChanges(p, olds, this.items);
+        }
+        // MultiPaper end - Sync player
     }
 
     // Paper start - EntityDeathEvent
diff --git a/net/minecraft/world/entity/TamableAnimal.java b/net/minecraft/world/entity/TamableAnimal.java
index efd6eabde047c897c9eb4366e23f23c1885ab866..43c944034237d0561b7aaa216b8fce1afbeb36d8 100644
--- a/net/minecraft/world/entity/TamableAnimal.java
+++ b/net/minecraft/world/entity/TamableAnimal.java
@@ -293,6 +293,14 @@ public abstract class TamableAnimal extends Animal implements OwnableEntity {
     }
 
     private boolean canTeleportTo(final BlockPos pos) {
+        // MultiPaper start - Sync player
+        // loads chunks at an owner for pets who owner is external player
+        if (this.getOwner() instanceof puregero.multipaper.ExternalPlayer externalPlayer) {
+            // use bukkit api to load that chunk aysnc
+            org.bukkit.craftbukkit.entity.CraftPlayer player = externalPlayer.getBukkitEntity();
+            player.getWorld().getChunkAtAsync(player.getLocation());
+        }
+        // MultiPaper end - Sync player
         PathType pathType = WalkNodeEvaluator.getPathTypeStatic(this, pos);
         if (pathType != PathType.WALKABLE) {
             return false;
diff --git a/net/minecraft/world/entity/player/Inventory.java b/net/minecraft/world/entity/player/Inventory.java
index cd7714dffcd7e7b6d96d18a97e65fd0914a739d1..79c0e315e0f8a1ef691c6378616d684662df6ff8 100644
--- a/net/minecraft/world/entity/player/Inventory.java
+++ b/net/minecraft/world/entity/player/Inventory.java
@@ -52,8 +52,12 @@ public class Inventory implements Container, Nameable {
         )
     );
     private static final Component DEFAULT_NAME = Component.translatable("container.inventory");
-    private final NonNullList<ItemStack> items = NonNullList.withSize(36, ItemStack.EMPTY);
-    private int selected;
+    // MultiPaper start - Sync player
+    // make public
+    // use NonNullListFilter instead of NonNullList
+    public final puregero.multipaper.NonNullListFilter<ItemStack> items;
+    public int selected;
+    // MultiPaper end - Sync player
     public final Player player;
     public final EntityEquipment equipment;
     private int timesChanged;
@@ -133,6 +137,7 @@ public class Inventory implements Container, Nameable {
     // Paper end - add fields and methods
 
     public Inventory(final Player player, final EntityEquipment equipment) {
+        this.items = puregero.multipaper.NonNullListFilter.withSize(36, ItemStack.EMPTY, player); // MultiPaper - Sync player - filter the inventory and sync it with other server
         this.player = player;
         this.equipment = equipment;
     }
@@ -437,6 +442,7 @@ public class Inventory implements Container, Nameable {
     @Override
     public ItemStack removeItem(final int slot, final int count) {
         if (slot < this.items.size()) {
+            this.items.markDirty(slot); // MultiPaper - Sync player
             return ContainerHelper.removeItem(this.items, slot, count);
         }
 
diff --git a/net/minecraft/world/entity/player/Player.java b/net/minecraft/world/entity/player/Player.java
index 8a65f720416a7f3bcef8cc4bf0734c27ef19a5d6..9b2247f9d5fb7947dc4ea7a6a2ddb009ce5ccab5 100644
--- a/net/minecraft/world/entity/player/Player.java
+++ b/net/minecraft/world/entity/player/Player.java
@@ -156,7 +156,7 @@ public abstract class Player extends Avatar implements ContainerUser {
     protected FoodData foodData = new FoodData();
     protected int jumpTriggerTime;
     public int takeXpDelay;
-    private int sleepCounter = 0;
+    public int sleepCounter = 0; // MultiPaper - Sync player - make public
     protected boolean wasUnderwater;
     private final Abilities abilities = new Abilities();
     public int experienceLevel = 0;
@@ -314,8 +314,20 @@ public abstract class Player extends Avatar implements ContainerUser {
             this.setPos(nx, this.getY(), nz);
         }
 
+    // MultiPaper start - Sync player
+        tickAttackStrengthAndItemSwap();
+        tickAttackStrengthAndItemSwapAfter();
+    }
+
+    public void tickAttackStrengthAndItemSwap() {
+    // MultiPaper end - Sync player
         this.attackStrengthTicker++;
         this.itemSwapTicker++;
+    // MultiPaper start - Sync player
+    }
+
+    private void tickAttackStrengthAndItemSwapAfter() {
+    // MultiPaper end - Sync player
         ItemStack mainHandItemStack = this.getMainHandItem();
         if (!ItemStack.matches(this.lastItemInMainHand, mainHandItemStack)) {
             if (!ItemStack.isSameItem(this.lastItemInMainHand, mainHandItemStack)) {
@@ -570,6 +582,7 @@ public abstract class Player extends Avatar implements ContainerUser {
     }
 
     private void touch(final Entity entity) {
+        if (puregero.multipaper.MultiPaperEntityInteractHandler.touchEntity(this, entity)) return; // MultiPaper - Sync player
         entity.playerTouch(this);
     }
 
@@ -2033,6 +2046,7 @@ public abstract class Player extends Avatar implements ContainerUser {
     }
 
     public void resetAttackStrengthTicker() {
+        puregero.multipaper.externalserverprotocol.PlayerResetAttackStrengthPacket.broadcastResetAttackStrength(this); // MultiPaper - Sync player
         this.attackStrengthTicker = 0;
         this.itemSwapTicker = 0;
     }
diff --git a/net/minecraft/world/entity/player/PlayerEquipment.java b/net/minecraft/world/entity/player/PlayerEquipment.java
index e59787c1698e96df98ef4cf98d62b4a67c4e95dc..1113101d2c137f39b5599886f0e749b216931952 100644
--- a/net/minecraft/world/entity/player/PlayerEquipment.java
+++ b/net/minecraft/world/entity/player/PlayerEquipment.java
@@ -5,7 +5,7 @@ import net.minecraft.world.entity.EquipmentSlot;
 import net.minecraft.world.item.ItemStack;
 
 public class PlayerEquipment extends EntityEquipment {
-    private final Player player;
+    public final Player player; // MultiPaper - Sync player - make public
 
     public PlayerEquipment(final Player player) {
         this.player = player;
diff --git a/net/minecraft/world/entity/projectile/arrow/ThrownTrident.java b/net/minecraft/world/entity/projectile/arrow/ThrownTrident.java
index 412576ab80763b55716db09fe3ff4126f0a0fec3..160688283233c31c44cb0de4b7369e94dabd26d7 100644
--- a/net/minecraft/world/entity/projectile/arrow/ThrownTrident.java
+++ b/net/minecraft/world/entity/projectile/arrow/ThrownTrident.java
@@ -206,7 +206,7 @@ public class ThrownTrident extends AbstractArrow {
 
     @Override
     public void playerTouch(final Player player) {
-        if (this.ownedBy(player) || this.getOwner() == null) {
+        if ((this.ownedBy(player) || this.getOwner() == null) && puregero.multipaper.MultiPaper.isChunkLocal(this.level().getChunkIfLoaded(this.blockPosition()))) { // MultiPaper - Sync player - ensure we're still ticking this entity
             super.playerTouch(player);
         }
     }
diff --git a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
index c635999ec8d5e0508cd66e0c4982692450963235..2875024bafd0a43869fb09643b7bff9b09757a07 100644
--- a/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
+++ b/net/minecraft/world/entity/vehicle/minecart/AbstractMinecart.java
@@ -346,8 +346,25 @@ public abstract class AbstractMinecart extends VehicleEntity {
         }
 
         this.firstTick = false;
+    // MultiPaper start - Sync player
+        this.getPushedByExternalPlayers();
     }
 
+    public void getPushedByExternalPlayers() {
+        java.util.List<Entity> list = this.level().getEntities(this, this.getBoundingBox(), net.minecraft.world.entity.EntitySelector.pushableBy(this));
+
+        this.numCollisions = Math.max(0, this.numCollisions - this.level().paperConfig().collisions.maxEntityCollisions);
+        for (int j = 0; j < list.size() && this.numCollisions < this.level().paperConfig().collisions.maxEntityCollisions; ++j) {
+            Entity entity = list.get(j);
+
+            if (puregero.multipaper.MultiPaper.isExternalPlayer(entity)) {
+                this.numCollisions++;
+                this.push(entity);
+            }
+        }
+    }
+    // MultiPaper end - Sync player
+
     public boolean isFirstTick() {
         return this.firstTick;
     }
diff --git a/net/minecraft/world/food/FoodData.java b/net/minecraft/world/food/FoodData.java
index eb838279d8c06296519b203fd598987d395c0740..4a65a5ed1d2917e4f3967f932900207f19ce1394 100644
--- a/net/minecraft/world/food/FoodData.java
+++ b/net/minecraft/world/food/FoodData.java
@@ -40,6 +40,7 @@ public class FoodData {
         org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(serverPlayer, foodProperties.nutrition() + oldFoodLevel, stack);
         if (!event.isCancelled()) {
             if (serverPlayer.level().purpurConfig.playerBurpWhenFull && event.getFoodLevel() == 20 && oldFoodLevel < 20) serverPlayer.burpDelay = serverPlayer.level().purpurConfig.playerBurpDelay; // Purpur - Burp after eating food fills hunger bar completely
+            puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(serverPlayer, new puregero.multipaper.externalserverprotocol.PlayerFoodUpdatePacket(serverPlayer)); // MultiPaper - Sync player
             this.add(event.getFoodLevel() - oldFoodLevel, foodProperties.saturation());
         }
         serverPlayer.getBukkitEntity().sendHealthUpdate();
@@ -53,12 +54,14 @@ public class FoodData {
             this.exhaustionLevel = this.exhaustionLevel - FoodConstants.EXHAUSTION_DROP;
             if (this.saturationLevel > 0.0F) {
                 this.saturationLevel = Math.max(this.saturationLevel - 1.0F, 0.0F);
+                puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(player, new puregero.multipaper.externalserverprotocol.PlayerFoodUpdatePacket(player)); // MultiPaper - Sync player
             } else if (difficulty != Difficulty.PEACEFUL) {
                 // CraftBukkit start
                 org.bukkit.event.entity.FoodLevelChangeEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callFoodLevelChangeEvent(player, Math.max(this.foodLevel - 1, 0));
 
                 if (!event.isCancelled()) {
                     this.foodLevel = event.getFoodLevel();
+                    puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(player, new puregero.multipaper.externalserverprotocol.PlayerFoodUpdatePacket(player)); // MultiPaper - Sync player
                 }
 
                 player.connection.send(new net.minecraft.network.protocol.game.ClientboundSetHealthPacket(player.getBukkitEntity().getScaledHealth(), this.foodLevel, this.saturationLevel));
@@ -132,11 +135,13 @@ public class FoodData {
         return this.saturationLevel;
     }
 
-    public void setFoodLevel(final int food) {
+    public void setFoodLevel(final ServerPlayer player, final int food, final boolean send) { // MultiPaper - Sync player
         this.foodLevel = food;
+        if (send) puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(player, new puregero.multipaper.externalserverprotocol.PlayerFoodUpdatePacket(player)); // MultiPaper - Sync player
     }
 
-    public void setSaturation(final float saturation) {
+    public void setSaturation(final ServerPlayer player, final float saturation, final boolean send) { // MultiPaper - Sync player
         this.saturationLevel = saturation;
+        if (send) puregero.multipaper.MultiPaper.broadcastPacketToExternalServers(player, new puregero.multipaper.externalserverprotocol.PlayerFoodUpdatePacket(player)); // MultiPaper - Sync player
     }
 }
diff --git a/net/minecraft/world/inventory/PlayerEnderChestContainer.java b/net/minecraft/world/inventory/PlayerEnderChestContainer.java
index 33c4c2cc8b488df5276d21e0f4e29cc0072a7682..c41d11207e830d1f716c46096a1edf5140e90657 100644
--- a/net/minecraft/world/inventory/PlayerEnderChestContainer.java
+++ b/net/minecraft/world/inventory/PlayerEnderChestContainer.java
@@ -12,6 +12,7 @@ import org.jspecify.annotations.Nullable;
 
 public class PlayerEnderChestContainer extends SimpleContainer {
     private @Nullable EnderChestBlockEntity activeChest;
+    public puregero.multipaper.@Nullable MultiPaperEnderChestHandler listener; // MultiPaper - Sync player
     // CraftBukkit start
     private final Player owner;
 
@@ -31,6 +32,16 @@ public class PlayerEnderChestContainer extends SimpleContainer {
         // CraftBukkit end
     }
 
+    // MultiPaper start - Sync player
+    @Override
+    public void setChanged() {
+        super.setChanged();
+        if (this.listener != null) {
+            this.listener.containerChanged(this);
+        }
+    }
+    // MultiPaper end - Sync player
+
     // Purpur start - Barrels and enderchests 6 rows
     @Override
     public int getContainerSize() {
diff --git a/net/minecraft/world/item/ItemStack.java b/net/minecraft/world/item/ItemStack.java
index 95f4d1a002e6da049657d5927800df15d5e4e686..32465c1de6c9078c71304a38d8cf2c07ff6f0756 100644
--- a/net/minecraft/world/item/ItemStack.java
+++ b/net/minecraft/world/item/ItemStack.java
@@ -156,6 +156,21 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
     @Deprecated
     private @Nullable Holder<Item> item;
     private PatchedDataComponentMap components;
+    // MultiPaper start - Sync player
+    public boolean dirty = true;
+
+    static {
+        EMPTY.dirty = false;
+    }
+
+    public puregero.multipaper.@Nullable NonNullListFilter<? extends ItemStack> listeningComponent;
+
+    public void markDirty() {
+        if (this == EMPTY || this.listeningComponent == null || this.listeningComponent.markDirty()) {
+            this.dirty = true;
+        }
+    }
+    // MultiPaper end - Sync player
 
     public static DataResult<ItemStack> validateStrict(final ItemStack itemStack) {
         DataResult<?> result = validateComponents(itemStack.getComponents());
@@ -990,15 +1005,25 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
     // CraftBukkit start
     public void restorePatch(DataComponentPatch datacomponentpatch) {
         this.components.restorePatch(datacomponentpatch);
+
+        this.markDirty(); // MultiPaper - Sync player
     }
     // CraftBukkit end
 
     public <T> @Nullable T set(final DataComponentType<T> type, final @Nullable T value) {
-        return this.components.set(type, value);
+        // MultiPaper start - Sync player
+        T t = this.components.set(type, value);
+        this.markDirty();
+        return t;
+        // MultiPaper start - Sync player
     }
 
     public <T> @Nullable T set(final TypedDataComponent<T> value) {
-        return this.components.set(value);
+        // MultiPaper start - Sync player
+        T t = this.components.set(value);
+        this.markDirty();
+        return t;
+        // MultiPaper start - Sync player
     }
 
     public <T> void copyFrom(final DataComponentType<T> type, final DataComponentGetter source) {
@@ -1015,7 +1040,11 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
     }
 
     public <T> @Nullable T remove(final DataComponentType<? extends T> type) {
-        return this.components.remove(type);
+        // MultiPaper start - Sync player
+        T t = this.components.remove(type);
+        this.markDirty();
+        return t;
+        // MultiPaper start - Sync player
     }
 
     public void applyComponentsAndValidate(final DataComponentPatch patch) {
@@ -1026,14 +1055,20 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
             LOGGER.error("Failed to apply component patch '{}' to item: '{}'", patch, validationError.get().message());
             this.components.restorePatch(oldPatch);
         }
+
+        this.markDirty(); // MultiPaper - Sync player
     }
 
     public void applyComponents(final DataComponentPatch patch) {
         this.components.applyPatch(patch);
+
+        this.markDirty(); // MultiPaper - Sync player
     }
 
     public void applyComponents(final DataComponentMap components) {
         this.components.setAll(components);
+
+        this.markDirty(); // MultiPaper - Sync player
     }
 
     // Paper start - (this is just a good no conflict location)
@@ -1340,6 +1375,8 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
 
     public void setCount(final int count) {
         this.count = count;
+
+        this.markDirty(); // MultiPaper - Sync player
     }
 
     public void limitSize(final int maxStackSize) {
diff --git a/net/minecraft/world/level/block/entity/BeaconBlockEntity.java b/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
index 9aabb4f2c852e160e31153153cd69e907bf91307..50c43b7ab06fb89545580c33aed51de2dc743c87 100644
--- a/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
@@ -301,7 +301,7 @@ public class BeaconBlockEntity extends BlockEntity implements MenuProvider, Name
         new io.papermc.paper.event.block.BeaconDeactivatedEvent(block).callEvent();
         // Paper end - beacon activation/deactivation events
         // Paper start - fix MC-153086
-        if (this.levels > 0 && !this.beamSections.isEmpty()) {
+        if (this.levels > 0 && !this.beamSections.isEmpty() && this.level.getChunkIfLoaded(this.worldPosition) != null && this.level.getChunkIfLoaded(this.worldPosition).loaded) { // MultiPaper - Sync chunk - only play beacon deactivate sound if we aren't unloading the chunk
         playSound(this.level, this.worldPosition, SoundEvents.BEACON_DEACTIVATE);
         }
         // Paper end
diff --git a/net/minecraft/world/level/block/state/BlockBehaviour.java b/net/minecraft/world/level/block/state/BlockBehaviour.java
index 720e6c9669ae0ca9fed7ff62de20f18203e2338b..4d9fac6c0dee8010d63bfad69f6abe9b70b41f59 100644
--- a/net/minecraft/world/level/block/state/BlockBehaviour.java
+++ b/net/minecraft/world/level/block/state/BlockBehaviour.java
@@ -937,6 +937,35 @@ public abstract class BlockBehaviour implements FeatureElement {
         }
 
         public InteractionResult useWithoutItem(final Level level, final Player player, final BlockHitResult hitResult) {
+            // MultiPaper start - Sync player
+            // open these menus on the player's server
+            if ((player instanceof puregero.multipaper.ExternalPlayer externalPlayer) && (
+                    this.getBlock() instanceof net.minecraft.world.level.block.CraftingTableBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.EnderChestBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.AnvilBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.LoomBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.CartographyTableBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.GrindstoneBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.SmithingTableBlock ||
+                            this.getBlock() instanceof net.minecraft.world.level.block.StonecutterBlock ||
+                            (this.getBlock() instanceof net.minecraft.world.level.block.BedBlock && !this.asState().getValue(net.minecraft.world.level.block.BedBlock.OCCUPIED)) // If it's occupied, we kick the villager out of the bed on the block's server
+            )) {
+                externalPlayer.externalServerConnection.send(new puregero.multipaper.externalserverprotocol.PlayerUseBlockPacket(player.getUUID(), hitResult));
+                return InteractionResult.CONSUME;
+            }
+            // open double chests on the double chests' primary server
+            if (this.getBlock() instanceof net.minecraft.world.level.block.ChestBlock chestBlock) {
+                net.minecraft.world.Container chestContainer = net.minecraft.world.level.block.ChestBlock.getContainer(chestBlock, this.asState(), level, hitResult.getBlockPos(), true);
+                if (chestContainer instanceof net.minecraft.world.CompoundContainer compoundContainer) {
+                    BlockEntity primaryChestEntity = puregero.multipaper.MultiPaperContainerHandler.getPrimaryChest(compoundContainer);
+                    ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder newChunkHolder = puregero.multipaper.MultiPaper.getChunkHolder((ServerLevel) primaryChestEntity.getLevel(), primaryChestEntity.getBlockPos());
+                    if (puregero.multipaper.MultiPaper.isChunkExternal(newChunkHolder)) {
+                        newChunkHolder.externalOwner.getConnection().send(new puregero.multipaper.externalserverprotocol.PlayerUseBlockPacket(player.getUUID(), hitResult));
+                        return InteractionResult.CONSUME;
+                    }
+                }
+            }
+            // MultiPaper end - Sync player
             return this.getBlock().useWithoutItem(this.asState(), level, hitResult.getBlockPos(), player, hitResult);
         }
 
diff --git a/net/minecraft/world/level/storage/PlayerDataStorage.java b/net/minecraft/world/level/storage/PlayerDataStorage.java
index eddb6ebe3fa50a818e54079b73ba117be0abd195..a3f8eecf3268daec985276472a661ea12e94f640 100644
--- a/net/minecraft/world/level/storage/PlayerDataStorage.java
+++ b/net/minecraft/world/level/storage/PlayerDataStorage.java
@@ -27,7 +27,7 @@ public class PlayerDataStorage {
     public PlayerDataStorage(final LevelStorageSource.LevelStorageAccess levelAccess, final DataFixer fixerUpper) {
         this.fixerUpper = fixerUpper;
         this.playerDir = levelAccess.getLevelPath(LevelResource.PLAYER_DATA_DIR).toFile();
-        this.playerDir.mkdirs();
+        // this.playerDir.mkdirs(); // MultiPaper - Sync player
     }
 
     public void save(final Player player) {
@@ -36,12 +36,14 @@ public class PlayerDataStorage {
             TagValueOutput output = TagValueOutput.createWithContext(reporter, player.registryAccess());
             player.saveWithoutId(output);
             Path playerDirPath = this.playerDir.toPath();
-            Path tmpFile = Files.createTempFile(playerDirPath, player.getStringUUID() + "-", ".dat");
+            java.io.ByteArrayOutputStream buffer = new java.io.ByteArrayOutputStream(); // MultiPaper - Sync player
             CompoundTag dataToStore = output.buildResult();
-            NbtIo.writeCompressed(dataToStore, tmpFile);
+            NbtIo.writeCompressed(dataToStore, buffer); // MultiPaper - Sync player
             Path realFile = playerDirPath.resolve(player.getStringUUID() + ".dat");
+            puregero.multipaper.MultiPaper.writeFile(realFile.toString(), buffer.toByteArray()); /* // MultiPaper - Sync player
             Path oldFile = playerDirPath.resolve(player.getStringUUID() + ".dat_old");
             Util.safeReplaceFile(realFile, tmpFile, oldFile);
+            */ // MultiPaper - Sync player
         } catch (Exception ignored) {
             LOGGER.warn("Failed to save player data for {}", player.getPlainTextName(), ignored); // Paper - Print exception
         }
@@ -63,6 +65,7 @@ public class PlayerDataStorage {
 
     private Optional<CompoundTag> load(final NameAndId nameAndId, final String suffix) {
         File realFile = new File(this.playerDir, nameAndId.id() + suffix);
+        /* // MultiPaper - Sync player
         // Spigot start
         boolean usingWrongFile = false;
         if (org.bukkit.Bukkit.getOnlineMode() && !realFile.exists()) { // Paper - Check online mode first
@@ -73,14 +76,24 @@ public class PlayerDataStorage {
             }
         }
         // Spigot end
-        if (realFile.exists() && realFile.isFile()) {
+        // MultiPaper start - Sync player
+        */
+        if (true) {
+        // MultiPaper end - Sync player
             try {
                 // Spigot start
-                Optional<CompoundTag> optional = Optional.of(NbtIo.readCompressed(realFile.toPath(), NbtAccounter.unlimitedHeap()));
+                // MultiPaper start - Sync player
+                byte[] bytes = puregero.multipaper.MultiPaper.readFile(realFile.toString());
+                if (bytes == null || bytes.length == 0) {
+                    return Optional.empty();
+                }
+                return Optional.of(NbtIo.readCompressed(new java.io.ByteArrayInputStream(bytes), NbtAccounter.unlimitedHeap()));
+                /* // MultiPaper end - Sync player
                 if (usingWrongFile) {
                     realFile.renameTo(new File(realFile.getPath() + ".offline-read"));
                 }
                 return optional;
+                */ // MultiPaper - Sync player
                 // Spigot end
             } catch (Exception ignored) {
                 LOGGER.warn("Failed to load player data for {}", nameAndId.name());
