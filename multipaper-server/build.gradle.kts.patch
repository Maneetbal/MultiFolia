--- a/purpur-server/build.gradle.kts
+++ b/purpur-server/build.gradle.kts
@@ -33,9 +_,33 @@
             outputDir = rootDirectory.dir("paper-server")
         }
     }
-    activeFork = purpur
+    // activeFork = purpur // MultiPaper
     // Purpur end - Rebrand
 
+    // MultiPaper start - Rebrand to MultiPaper
+    val multipaper = forks.register("multipaper") {
+        forks = purpur
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = purpur.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("multipaper-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+        upstream.patchDir("purpurServer") {
+            upstreamPath = "purpur-server"
+            excludes = setOf(
+                "src/minecraft",
+                "paper-patches",
+                "minecraft-patches",
+                "build.gradle.kts",
+                "build.gradle.kts.patch"
+            )
+            patchesDir = rootDirectory.dir("multipaper-server/purpur-patches")
+            outputDir = rootDirectory.dir("purpur-server")
+        }
+    }
+    activeFork = multipaper
+    // MultiPaper end - Rebrand to MultiPaper
+
     /*updatingMinecraft { // Purpur start - TODO - snapshot - we don't need this
         // oldPaperCommit = "d4fe85375af18bfa88f44d7c1e6a61904ae550cc"
     }*/ // Purpur end - TODO - snapshot - we don't need this
@@ -105,10 +_,14 @@
     main {
         java { srcDir("../paper-server/src/main/java") }
         resources { srcDir("../paper-server/src/main/resources") }
+        java { srcDir("../purpur-server/src/main/java") }
+        resources { srcDir("../purpur-server/src/main/resources") }
     }
     test {
         java { srcDir("../paper-server/src/test/java") }
         resources { srcDir("../paper-server/src/test/resources") }
+        java { srcDir("../purpur-server/src/test/java") }
+        resources { srcDir("../purpur-server/src/test/resources") }
     }
 }
 val log4jPlugins = sourceSets.create("log4jPlugins") {
@@ -137,7 +_,12 @@
 }
 
 dependencies {
-    implementation(project(":purpur-api")) // Purpur
+    // MultiPaper start - Rebrand to MultiPaper
+    implementation(project(":multipaper-api"))
+    implementation(project(":multipaper-mastermessagingprotocol"))
+    implementation("io.methvin:directory-watcher:0.19.1")
+    implementation("com.github.luben:zstd-jni:1.5.7-4")
+    // MultiPaper end - Rebrand to MultiPaper
     implementation("ca.spottedleaf:leafpile:1.0.0")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -203,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Purpur", // Purpur
+            "Implementation-Title" to "MultiPaper", // Purpur // MultiPaper
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Purpur", // Purpur
+            "Specification-Title" to "MultiPaper", // Purpur // MultiPaper
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Purpur Team", // Purpur
-            "Brand-Id" to "purpurmc:purpur", // Purpur
-            "Brand-Name" to "Purpur", // Purpur
+            "Specification-Vendor" to "MultiPaper", // Purpur // MultiPaper
+            "Brand-Id" to "multipaper:multipaper", // Purpur // MultiPaper
+            "Brand-Name" to "MultiPaper", // Purpur // MultiPaper
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
